# Workflow Requirements

Status: Draft
Related Features: auth, organization, agent-builder, audit-tracing, knowledge, deployment, chatbot-deployment, mail-credentials, llm-credentials, conversation-memory

## Purpose

Workflow feature는 사용자가 업무 절차를 노드 그래프로 구성하고, 수동 실행·스케줄·웹훅·API trigger로 실행할 수 있게 한다. 이 문서는 전체 workflow runtime 요구사항 중 다른 도메인과 충돌하기 쉬운 권한·실행 주체·감사 경계를 우선 기록한다.
워크플로우 생성, 편집, 테스트 실행, 배포의 기본 사용자 흐름을 제공한다. 빌더는 캔버스에서 노드를 조합하고, 테스트 실행으로 각 노드가 정상 동작하는지 확인한 뒤 배포로 이어간다.
MBA-104 범위에서는 비용 최적화와 A/B 비교 실행을 준비하기 위해 테스트 실행 UX를 먼저 정리한다. 테스트 버튼을 눌렀을 때 테스트 실행 사이드바에서 각 노드의 실행 상태, 소요 시간, 비용, 토큰 사용량을 한눈에 확인할 수 있어야 한다.


## User Stories

### 1. 실행 편의성
- 빌더로서, 테스트 실행 중 어떤 노드가 실행 중이고 어떤 노드가 완료/실패했는지 테스트 실행 사이드바에서 확인하고 싶다.
- 빌더로서, 각 노드별 소요 시간, 비용, 토큰 사용량을 빠르게 비교해 병목 또는 비용이 큰 노드를 찾고 싶다.
- 빌더로서, 워크플로우 테스트가 모두 끝난 뒤 서버에서 실제 워크플로우가 실행된 시간과 브라우저 화면에서 완료까지 체감한 시간을 함께 확인하고 싶다.
- 빌더로서, 워크플로우 테스트가 끝난 뒤 최종 사용자가 실제로 받게 되는 응답 메시지를 노드별 JSON 상세를 열기 전에도 한눈에 확인하고 싶다.
- 빌더로서, 전체 비용과 전체 토큰 사용량은 서버 실행 결과 기준으로 확인하고 싶다.
- 빌더로서, 향후 모델/프롬프트 A/B 비교를 붙이기 전에 기본 단일 실행 결과부터 명확하게 보고 싶다.
- 빌더로서, 긴 입력값과 노드별 실행 상세를 읽을 때 테스트 실행 사이드바를 필요한 만큼 넓히고 싶다.
- 빌더로서, 보고 화면을 확인하고 돌아오거나 브라우저를 새로고침해도 가장 최근 테스트 실행의 노드별 결과와 선택한 상세를 다시 보고 싶다.


- 빌더로서, 배포 전에 필요한 credential과 권한 누락을 확인하고 싶다.
- 운영자로서, schedule/webhook/API trigger로 실행된 workflow가 명시 실행 주체 권한 또는 anonymous public-only 경계 중 무엇으로 RAG retrieval을 수행했는지 추적하고 싶다.
- 감사자로서, workflow owner와 실제 execution subject를 구분해 audit/trace에서 확인하고 싶다.

### 2. 노드 조작 편의성
- 빌더로서, 노드 상세 편집 화면에서 왼쪽/가운데/오른쪽 3패널의 가로 비율을 작업 맥락에 맞게 조정하고 싶다.
- 빌더로서, 프롬프트나 코드처럼 긴 내용을 편집할 때 왼쪽 편집 패널을 넓히되, 가운데 미리보기/캔버스와 오른쪽 보조 패널이 완전히 사라지지 않기를 원한다.

### 3. 워크플로우 조작 편의성
- 빌더로서, 선택한 노드를 Backspace 또는 Delete 키로 빠르게 삭제하고 싶다.
- 빌더로서, 중간 노드를 삭제했을 때 앞단과 뒷단 노드가 자연스럽게 다시 연결되어 워크플로우 흐름이 끊기지 않기를 원한다.

### 4. 노드 실행 기록 패널 추가
- 빌더로서, 노드 상세 패널에서 현재 노드의 과거 실행 input/output을 바로 참고하고 싶다.
- 빌더로서, 전체 워크플로우 실행 로그 중 현재 노드 기록이 포함된 실행을 검색/필터해 선택하고 싶다.
- 빌더로서, 가장 최신 실행 기록을 빠르게 불러와 현재 노드가 실제로 어떤 입력을 받고 어떤 출력을 냈는지 확인하고 싶다.
- 빌더로서, 과거 실행 기록을 참고하되 현재 노드 설정값과 과거 input/output을 혼동하지 않기를 원한다.

## Functional Requirements
- FR-001: Workflow run context는 organization, workflow, workflow version, run id, node id, trigger mode, actor 또는 service account 정보를 전달한다.
- FR-002: Interactive 실행은 요청 사용자를 execution subject로 사용할 수 있다.
- FR-003: Schedule, webhook, API trigger처럼 요청 사용자가 명확하지 않은 실행은 MVP에서 `execution_subject` 없이 anonymous public-only RAG로 실행한다. Private KB access가 필요한 자동 실행은 후속 service account, assigned operator, 또는 별도 정책으로 확정된 execution subject 기능이 필요하다.
- FR-004: RAG 옵션이 켜진 LLM node가 Knowledge retrieval을 호출할 때 workflow runtime은 로그인 interactive 실행에서는 `execution_subject=current_user`를 전달한다. 비로그인/자동 실행처럼 `execution_subject`가 없으면 anonymous public-only로 처리한다.
- FR-005: `execution_subject`가 없을 때 Workflow owner, deployment owner, app creator, builder, `user_id` 권한으로 조용히 fallback하지 않는다. Subject 부재는 private retrieval 실패가 아니라 anonymous public-only gate를 통과한 public collection/KB 후보만 허용하는 실행이다. Source-managed KB는 collection public visibility와 별도 source/connector public exposure approval을 모두 통과해야 한다. 모호하거나 지원하지 않는 subject는 private retrieval fail-closed로 처리한다.
- FR-006: Workflow owner, deployment owner, execution subject는 audit/trace에서 구분할 수 있어야 한다. Owner는 소유권과 관리 표시에는 사용할 수 있지만, 명시 정책 없이 실행 시점 data access 권한으로 사용하지 않는다.
- FR-007: Workflow runtime이 Knowledge Skill을 사용할 경우, execution subject 기준으로 skill visibility, freshness/eval, collection route, KB permission/source ACL gate를 통과해야 한다. 빌더 단계 skill 선택이나 workflow 작성자 권한은 실행 시점 data access 권한으로 전파되지 않는다.
- FR-008: LLM node의 RAG 옵션을 포함한 workflow 배포는 deployment type에서 파생한 runtime audience 기준 preflight를 수행해야 한다. 사용자 subject가 없는 API/webapp/widget/webhook/schedule/공개 `chatbot`/MCP surface는 private KB 후보를 활성 배포로 올릴 수 없고 anonymous public-only 후보만 허용한다. `internal_chatbot`은 server-derived `authenticated_user` audience를 사용하며 실행 시 current user 기준 KB permission/source ACL을 다시 검사한다.
- FR-009: Workflow-node 실행은 parent workflow의 execution context를 상속한다. Parent execution subject가 있으면 해당 subject 기준 KB permission/source ACL gate를 사용하고, subject가 없으면 anonymous public-only로 낮춘다. Deployment preflight에서 workflow-node target은 node 설정의 `workflowNode.data.appId`를 기준으로 target app active deployment를 찾는다.
- FR-009a: 공개 `chatbot`과 `internal_chatbot`은 서버가 memory mode를 강제한다. 인증 실행의 client `conversation_id`는 deployment와 execution subject를 포함한 namespace로 변환하며 상세 계약은 [chatbot-deployment](../chatbot-deployment/requirements.md)를 따른다.
- FR-010: System schedule execution은 App/deployment creator나 workflow owner를 executor로 기록하지 않아야 한다. Canonical schedule claim과 연결된 `WorkflowRun.user_id`는 null이고 audit actor는 system이어야 하며, 기존 manual/API/webhook interactive run의 사용자 attribution은 유지해야 한다.
- FR-011: Schedule Worker admission winner는 claim을 `running`으로 전이하는 같은 원자적 write에서 stable workflow run id를 생성·저장하고 duplicate delivery에서 같은 id를 재사용해야 한다. Admission 이후 실패나 timeout으로 outcome이 불명확하면 engine을 자동 재실행하지 않아야 한다.
- FR-012: Schedule occurrence idempotency key는 MBA-187 dispatch/admission 계층의 correlation으로만 유지하고 `ExternalEffectContext`, node/provider adapter 또는 provider-visible key 입력으로 전달하지 않아야 한다. MBA-190 external effect identity는 canonical claim UUID에서 별도로 계산한 `execution_id`를 사용하며 prompt, user-visible output, durable raw trace와 metric label에 schedule key나 새 identity를 복제하지 않아야 한다. 어느 값도 외부 provider의 exactly-once를 보장하지 않는다.
- FR-013: 요청 사용자가 없는 system schedule이 LLM provider credential을 사용해야 할 때 runtime은 locked canonical deployment의 `created_by`를 user형 credential principal로만 사용할 수 있다. 이 principal은 queue 입력에서 받지 않으며 executor, audit actor 또는 Knowledge execution subject로 승격하지 않는다. Legacy `LLMUsageLog.user_id`에는 비용/credential 귀속을 위해 이 principal을 기록하되 WorkflowRun actor 의미로 해석하지 않는다. 별도 service account principal은 lifecycle과 권한 모델이 승인되기 전까지 합성하지 않는다.
- FR-014: Code node는 sandbox tenant/fairness context에 canonical `execution_context.organization_id`를 전달해야 한다. `user_id`를 organization tenant로 해석하거나 system schedule의 null executor 때문에 canonical organization을 누락해서는 안 된다.
- FR-015: RAG retrieval 및 evidence policy block audit의 user actor는 실제 user형 `execution_subject`에서만 가져온다. System schedule은 `actor_id=NULL`, `actor_type=system`으로 기록하며, credential principal은 query embedding/LLM credential 선택에 사용할 수 있지만 RAG audit actor로 승격하지 않는다.
- FR-016: 실행 전 Knowledge sync는 best-effort 전처리다. Connector 또는 DB 동기화가 일시적으로 실패해도 이미 색인된 evidence로 Workflow Engine 실행을 계속하며, task 결과와 로그에는 raw 예외 없이 safe sync failure reason만 남긴다.
- FR-017 (Conversation Memory Target Integration): Memory-enabled task envelope은 memory contract/storage generation과 minimum Worker capability를 포함해야 한다. Worker는 외부 node side effect 전에 이를 검증하고 rolling migration에서는 capability 전용/versioned queue를 사용해야 한다. Preflight는 runtime task 검증을 대체하지 않는다.
- FR-018 (Conversation Memory Target Integration): Duplicate Memory dispatch는 Memory-owned write를 idempotent하게 처리해야 한다. Tool/connector/custom node의 외부 side effect 재시도·중복 방지는 Workflow node별 idempotency 계약이 소유하며 Memory dispatcher가 arbitrary workflow execution을 무조건 재실행해서는 안 된다.
- FR-019 (Conversation Memory Target Integration): Workflow application은 `AdmitExecution(dispatch_id)`을 durable/idempotent하게 수행해 같은 dispatch의 execution admission을 최대 하나만 생성하고 existing admission reference를 재반환해야 한다.
- FR-020 (Conversation Memory Target Integration): Workflow application은 Memory reconciler가 acknowledgement 유실을 복구할 수 있는 `GetExecutionAdmission(dispatch_id)` port를 제공해야 한다. Workflow execution lease/heartbeat와 retry policy는 Workflow가 소유하고 Memory turn state는 safe projection만 받아야 한다.
- FR-021 (Conversation Memory Target Integration): 모든 content-bearing node result는 server-derived `RuntimeDataDependencyEnvelope`와 completeness marker를 함께 전달해야 한다. Knowledge/connector/tool/system policy adapter는 자기 source dependency를 발급하고, subworkflow는 target deployment version과 child envelope 합집합을 반환해야 한다. Condition/Switch/Loop runtime은 predicate, selected route, iterable, bound와 termination에 사용한 dependency를 active control context로 전파하고 최종 envelope은 값 dependency와 활성 control dependency의 합집합을 가져야 한다. Producer가 값·제어 source 영향이 없음을 확인한 explicit complete empty envelope만 허용하고 missing/unknown envelope을 empty로 간주해서는 안 된다.
- FR-022 (Conversation Memory Target Integration): Transform/code/LLM/final output은 내용에 영향을 준 모든 input dependency의 합집합을 보존해야 한다. LLM output은 prompt input뿐 아니라 Memory Context, retrieval과 tool dependency도 상속해야 한다. V1은 optional dependency를 지원하지 않으며 node/client가 canonical dependency를 삭제·발급하거나 required 의미를 낮출 수 없어야 한다.
- FR-023 (Conversation Memory Target Integration): Provenance를 보존하지 못한 code/custom output은 public-only/non-sensitive라는 server-side 증명이 없으면 private/sensitive Memory write에서 fail-closed해야 한다.
- FR-024 (Conversation Memory Target Integration): Memory task/admission은 deployment ID와 immutable version 또는 snapshot hash, conversation mapping/Memory policy version, contract/storage generation을 고정해야 한다. Worker가 current active deployment pointer로 기존 session task를 자동 rebind해서는 안 된다.
- FR-025 (Conversation Memory Target Integration): Main/summary/query-embedding provider adapter는 LLM Credentials domain이 발급한 opaque `ProviderExecutionCapability` identity/revision과 자기 operation의 deployment version, canonical node location, node invocation, execution admission, provider attempt와 purpose binding을 검증해야 한다. Workflow는 credential principal, permission decision revision 또는 capability scope를 자체 구성하지 않아야 한다.
- FR-025a (Provider Capability Target Activation): `provider_execution_capability_required` server runtime path의 LLM node는 trusted `NodeExecutionControl`의 canonical organization/workflow/node invocation과 exact deployment/version, explicit user·anonymous-public·system execution identity, organization billing principal, bounded token/cost cap을 갖춘 경우에만 LLM Credentials issuer를 호출해야 한다. 해당 path는 graph/inputs의 credential ID, legacy `user_id`/owner, model routing/fallback/default candidate를 credential selection에 사용하지 않으며 policy가 고른 model과 다른 provider call을 시작해서는 안 된다. Main generation은 실제 prompt UTF-8 byte upper bound와 generic output limit을 admission 요청량으로 전달하고 `n`과 `best_of`가 정확한 정수 `1`인 단일 생성만 허용한다. Query embedding은 authorized 후보가 0개면 capability와 provider operation을 만들지 않고, 후보가 있으면 distinct immutable embedding model별 exact query policy와 sealed request bound를 final-admit한 뒤 durable intent/start fence를 사용한다. Shared issuer는 canonical pricing 최대 비용과 함께 cap을 검증한 뒤 capability를 provider call 전에 commit한다. Conversation Session/Access Grant/lease authorization이 없는 legacy inline memory summary는 이 path에서 skip하며 별도 Memory adapter가 distinct `memory_summary` capability를 consume하기 전에는 summary provider call을 만들지 않는다. Legacy execution의 전면 activation은 MBA-320이 소유한다.
- FR-025b (Durable Provider Usage): Capability-required LLM call은 [ADR-0069](../../decisions/ADR-0069-provider-usage-durable-ledger.md)의 canonical operation을 사용해야 한다. Final admission 뒤 intent와 provider-start fence가 각각 commit된 후에만 opaque lease를 호출하고, success·명시적 zero-effect failure·outcome unknown을 같은 organization/provider-attempt/purpose key로 수렴시켜야 한다. Provider-start 뒤 결과 불명, malformed/상한 초과 usage 또는 terminal 저장 실패는 자동 provider retry/fallback 없이 즉시 unknown으로 종료하고, typed `before_send`만 `failed_definitive/provider_not_sent`로 닫는다. Immutable price는 DB와 같은 fixed scale로 exact-normalize한다. WorkflowRun 생성과 compatibility projection 지연·동시 실행은 canonical token/cost 또는 run aggregate delta를 버리지 않아야 하며 public/system execution subject와 audit actor를 credential principal로 대체하지 않아야 한다. Canonical operation이 남아 있는 schema downgrade는 이력 삭제 전에 중단해야 한다.
- FR-026 (Conversation Memory Target Integration): Execution subject, credential principal, billing principal, audit actor와 Conversation Access Grant는 runtime context에서 명시적으로 구분해야 한다. Public grant나 app/deployment owner를 Knowledge subject 또는 audit actor로 승격하지 않아야 한다.
- FR-027 (MBA-190): 외부 부수효과를 만드는 provider operation은 versioned contract와 함께 외부 서비스의 중복 방지 지원 여부(`supported`, `unsupported`, `unknown`)와 안전한 결과 재사용 가능 여부(`supported`, `unavailable`)를 서로 독립적으로 선언해야 한다. Contract는 key 전달 위치·header field 이름 또는 canonical body JSON Pointer·형식·최대 길이·보존 기간, effect success/rejection 판정, duplicate/conflict/response reuse 의미와 공식 근거를 포함해야 한다. 공식 문서에서 확인할 수 없는 값은 추측하거나 사용자 입력으로 보충하지 않아야 한다. 외부 서비스 중복 방지 `supported`는 key 전달 위치가 `header|body`이고 이 공식 계약과 adapter integration test가 모두 준비된 경우에만 허용하며 `none|unknown` transport나 Generic HTTP의 사용자 지정 idempotency header만으로 승격해서는 안 된다. 결과 재사용 `supported`는 allowlist 적용 뒤 FR-034의 canonical JSON UTF-8 전체 크기가 65,536 bytes와 provider별 더 작은 한도를 모두 통과하고 최초 성공과 중복 전달에 같은 node output schema를 제공할 수 있을 때만 허용한다. Contract version은 추가만 가능하고 한 번 사용한 과거 version의 의미를 바꾸거나 제거해서는 안 된다.
- FR-028 (MBA-190): Draft test, legacy deployed, deployment ID 기반 API/public/webhook, schedule, stream과 subworkflow는 공통 `execution_id` 생성·검증 계약을 사용해야 한다. 최초 publisher 또는 직접 실행 조정자가 logical execution마다 한 번 발급하고 retry와 duplicate delivery는 이를 유지해야 한다. Compare A/B variant는 의도적으로 서로 다른 logical execution이므로 각각 다른 `execution_id`를 발급하고 각 variant의 retry에서만 최초 값을 유지해야 한다. 각 진입점은 queue/client 값을 source of truth로 사용하지 않고 canonical DB resource에서 `organization_id`, 현재 node의 `app_id`와 `workflow_id`를 확인해야 한다. Retry payload에 identity나 resource provenance가 없으면 새 값을 생성하거나 owner로 fallback하지 않고 provider 호출 전에 실패해야 한다. Subworkflow는 부모 `execution_id`를 유지하고 target deployment의 app/workflow provenance와 Loop iteration·호출 경로를 구분하는 별도 `node_invocation_id`를 사용해야 한다. Full external effect identity는 organization scope 안의 `execution_id + node_invocation_id + operation + effect_sequence`지만 DB unique는 operation 변경으로 새 row를 우회하지 못하도록 `organization_id + execution_id + node_invocation_id + effect_sequence` stable slot을 사용해야 한다. `effect_sequence`는 operation별이 아닌 invocation 전체의 0-based 외부 작업 순번이며 현재 1회 호출 node는 `0`이어야 한다. `workflow_run_id`, `node_run_id`, graph `node_id`는 uniqueness에 사용하지 않아야 한다. 현재 저장소 내부 발행자가 없는 legacy `workflow.execute_deployed`는 기존 positional 인자를 유지하되 최초 message의 additive optional envelope로 publisher-issued `execution_id`, exact `deployment_id`, `deployment_version`, canonical `snapshot_sha256`를 모두 받아야 한다. Worker는 같은 hash의 다른 deployment도 대체하지 않고 exact row를 검증해야 한다. Worker가 최초 active deployment를 골라 retry 인자에만 추가하는 방식은 원본 broker redelivery를 고정하지 못하므로 금지한다. Envelope 누락 호출은 external effect가 없는 graph에서만 기존 동작을 유지할 수 있고 effect boundary에서는 새 identity/snapshot binding을 만들지 않은 채 provider 전에 실패해야 한다.
- FR-029 (MBA-190): Node invocation별 `ExternalEffectContext`는 server-verified `organization_id`, 현재 node의 `app_id`/`workflow_id`와 invocation identity를 가진 immutable value여야 한다. Application use case의 별도 typed control parameter로만 전달하고 workflow `inputs`, template/Jinja variable map, LLM prompt/tool input, node output 또는 병렬 node가 공유하는 mutable execution context에 병합하거나 덮어쓰지 않아야 한다. DB session을 context에 넣거나 병렬 node 사이에서 같은 SQLAlchemy session을 공유하지 않아야 한다. Provider contract는 server가 검증한 logical `NodeSchema.type`과 canonical action으로 선택해야 하며 Python node class, URL, header나 client-supplied provider 값으로 선택하거나 덮어써서는 안 된다. `slackPostNode`는 기존 `HttpRequestNodeData`와 사용자가 바꿀 수 있는 API endpoint/incoming webhook 요청 동작을 유지해야 하며 MBA-190에서 새 mode/method/target 제한을 추가해서는 안 된다. 모든 Slack request는 endpoint, method나 `slackMode`에 무관하게 하나의 보수적 `slack.http.request` profile을 사용하고 URL, mode, method나 header로 capability를 `supported`로 올릴 수 없어야 한다. GitHub는 `comment_pr`만 external effect로 분류해야 한다. Application은 stable effect slot을 먼저 조회해 기존 row가 있으면 그 row의 frozen provider contract profile을, 없으면 server-derived provider/operation의 active profile을 선택해야 한다. Adapter는 선택된 contract version으로 provider key를 제외한 실제 effect-semantic request를 canonicalize한 SHA-256 `effect_input_digest`를 계산해야 한다. Canonical input은 method, rendered target, 최종 명시 header/body와 인증 값을 포함하되 원문은 digest 입력으로만 사용하고 저장하지 않아야 하며, header/JSON 정규화와 검증 실패 stage tag를 contract version이 고정해야 한다. 동시 insert 충돌도 winner row를 다시 읽고 loser가 선택한 contract version까지 비교해야 한다. 최초 row가 app/workflow provenance, provider, operation, contract version, 두 지원 수준과 digest를 고정하며 operation을 포함한 재진입 값이 하나라도 다르면 별도 row를 만들거나 provider/result/downstream을 사용하지 않고 non-retryable `external_effect.identity_conflict`로 종료해야 한다.
- FR-030 (MBA-190): Provider 호출 전 `workflow_node_effect_attempts`를 `prepared`로 만들고 owner/expiry/generation claim을 획득해야 한다. `claim_owner`는 claim/reopen마다 CSPRNG UUID로 새로 만드는 delivery-local opaque token이어야 하며 Celery task ID, worker hostname, execution identity나 retry count에서 파생해서는 안 된다. 같은 Celery task ID의 retry/redelivery도 다른 token을 사용해야 한다. Provider 실행 권한을 가진 delivery의 `prepared -> in_flight|terminal`과 `in_flight -> terminal` mutation은 claim 응답의 owner token, generation과 아직 유효한 expiry를 함께 비교해야 한다. 만료 복구는 예상 status/generation과 `claim_expires_at <= DB clock`, terminal reopen·budget 소진 전이는 terminal status/generation/decision과 active claim이 없음을 비교해야 하며 token을 attempt column 밖의 log/trace/metric/output에 노출해서는 안 된다. `prepared` 생성·claim, `in_flight` 전이와 `terminal` 확정은 session factory에서 매번 새 session을 받아 독립된 짧은 transaction으로 commit해야 한다. `in_flight` commit 실패 시 provider를 호출하지 않고 network I/O 동안 DB session, transaction 또는 row lock을 유지하지 않아야 한다. Provider 결과를 받은 뒤 terminal update의 generation compare-and-set이나 commit이 실패하면 node output과 downstream을 사용하지 않아야 하며, terminal commit 성공 뒤에만 최초 output 또는 replay result를 Workflow Engine에 반환해야 한다. 새 claim TTL은 active Workflow Engine Celery hard time limit에 code-owned terminal commit 여유 30초를 더해 계산하고 새 환경변수를 추가하지 않아야 한다. 현재 hard limit 600초에서는 630초여야 하며 hard limit이 없거나 양수가 아니면 Worker readiness가 실패해야 한다. DB claim 판정은 DB clock, task 대기 제한은 task 시작 때 계산한 monotonic deadline을 사용해야 한다. 유효한 claim을 다른 Worker가 소유하면 중복 Worker는 provider를 호출하거나 즉시 성공/실패하지 않고, session/lock을 유지하지 않는 조회를 100ms에서 최대 1초 간격으로 늘리면서 terminal, claim 만료 또는 기존 task deadline 중 먼저 오는 경계까지만 기다려야 한다. Terminal이면 frozen provider/contract/digest를 확인하고 `reuse_result|result_unavailable|stop`은 저장 decision을 따르며, `retry_before_effect|replay_same_key`는 같은 진입에서 provider/reopen 없이 internal retry-permitted 결과를 기존 Celery retry 경계에 반환해야 한다. 기존 retry budget/backoff가 허용한 다음 delivery만 row를 reopen할 수 있다. 정책이 retry를 거부/소진하면 terminal outcome, terminal timestamp와 기존 allowlisted `error_code`를 유지하고 decision만 `stop`으로 원자적 compare-and-set해 이후 broker redelivery가 reopen하지 못하게 해야 한다. 이때 outcome unknown은 `external_effect.outcome_unknown`, failed-before-effect는 기존 safe provider failure로 현재 task를 종료하고 provider를 호출해서는 안 된다. 만료된 `in_flight`는 상태 정리만 수행하고, 만료된 `prepared`는 row 변경·claim 없이 대기를 끝내며, 어느 경우에도 같은 진입에서 provider를 호출하지 않아야 한다. Task deadline이 먼저 오면 row를 변경하지 않고 기존 timeout/retry 경계로 종료해야 한다. Outcome과 replay decision은 terminal에서만 non-null이며 replay decision은 `reuse_result`, `result_unavailable`, `retry_before_effect`, `replay_same_key`, `stop`으로 제한한다.
- FR-031 (MBA-190): Workflow Runtime은 provider 호출 결과를 `succeeded`, `failed_before_effect`, `effect_outcome_unknown` 중 하나로 분류해야 한다. Adapter만 요청 전송 여부와 일시/영구 실패를 typed result로 판정하고 공통 application policy는 raw status/body/exception을 다시 해석하지 않아야 한다. Generic HTTP local validation/serialization과 GitHub issue comment의 완전한 `401`, `403`, `404`, `410`, `422` rejection은 `failed_before_effect + stop`, 요청 전송 전 일시 실패임이 증명된 경우만 `failed_before_effect + retry_before_effect`, 전송 여부를 증명할 수 없는 timeout/disconnect/response loss는 `effect_outcome_unknown`이어야 한다. GitHub `comment_pr`의 `201` 아닌 완전한 응답 중 위 다섯 rejection status가 아닌 `2xx`, `4xx`, `429`, `5xx`는 effect 부재를 증명할 수 없으므로 `effect_outcome_unknown + stop`이어야 한다. 명시적으로 pre-send/local allowlist에 없는 provider library exception, adapter bug와 새 exception subclass는 `in_flight` 이후 기본 `effect_outcome_unknown + stop`으로 닫고 generic Celery retry에 넘겨서는 안 된다. 만료된 `prepared`는 다음 동일 execution task만 새 generation으로 이어서 처리한다. 별도 주기적 recovery task나 scheduler를 추가하지 않고, 같은 logical execution이 재진입할 때 자기 attempt의 만료된 `in_flight`만 같은 status/generation/expiry 조건으로 `effect_outcome_unknown` terminal 상태로 바꿔야 한다. 이 update는 고정된 provider replay capability와 저장된 `replay_deadline_at`에 따라 `replay_same_key` 또는 `stop`을 기록하되 provider 호출이나 workflow 신규 실행을 하지 않아야 하며 stale generation Worker의 갱신을 거부해야 한다. Claim fencing은 이미 시작된 network I/O를 취소하지 않으므로 만료 뒤 `supported` same-key replay와 이전 요청이 겹칠 수 있고, 이때 보장하는 것은 동시 요청 금지가 아니라 provider 계약에 따른 duplicate effect 방지다. MBA-190은 heartbeat, 원격 요청 취소, Celery 전체 retry 횟수, backoff와 delivery/ack 정책을 변경하지 않는다.
- FR-032 (MBA-190): `effect_outcome_unknown + provider replay supported`는 최초 provider key와 contract version을 그대로 사용하는 `replay_same_key`만 허용하고, `unsupported/unknown`은 `stop`으로 닫아 자동 재호출을 막아야 한다. `retry_before_effect`와 DB clock이 저장된 `replay_deadline_at`보다 이른 `replay_same_key`만 identity, 두 지원 수준, key, contract와 deadline을 보존한 같은 row를 새 claim generation의 `prepared`로 원자적으로 다시 열 수 있다. Reopen은 예상 terminal status/decision/generation과 `claim_owner|claim_expires_at IS NULL`을 조건으로 새 owner/expiry와 증가한 generation을 같은 transaction에 기록해야 하며 이전 claim의 유효 expiry를 요구해서는 안 된다. `reuse_result`, `result_unavailable`, `stop`은 다시 열 수 없어야 한다. DB CHECK는 nullability, enum 조합과 generation 범위처럼 시간이 지나도 변하지 않는 구조만 검사하고, claim과 deadline 만료는 mutation이 DB clock으로 검사해야 한다.
- FR-033 (MBA-190): Provider replay가 `supported`이고 key 전달 위치가 `header|body`인 operation만 provider-visible key를 전용 versioned HMAC secret과 canonical identity로 생성·전달해야 한다. Key canonical input은 organization/app/workflow provenance와 `execution_id`, `node_invocation_id`, operation, effect sequence를 length-delimited encoding으로 포함해야 한다. Attempt는 이 경우에만 key version/format/fingerprint와 attempt 생성 DB 시각·당시 retention으로 계산한 `replay_deadline_at`을 저장하고 `unsupported|unknown`에서는 네 값을 null로 유지해야 한다. `unsupported|unknown` 또는 key 전달 위치 `none|unknown`에서는 시스템 key를 생성·주입하거나 사용자 header/body를 덮어쓰지 않아야 한다. Retry에 필요한 과거 key나 contract version이 없으면 새 key 또는 현재 contract로 대체하지 않고 fail-closed해야 한다. 최초 insert 경쟁에서 같은 contract/digest를 준비한 Worker의 active key version이 다르면 winner row의 key version/format/fingerprint를 source of truth로 사용해야 한다. Loser candidate key는 전송해서는 안 되며, 실제 claim owner는 winner row의 key를 재생성해 fingerprint와 provider 길이를 검증한 뒤에만 최종 provider request에 한 번 주입해야 한다.
- FR-034 (MBA-190): `succeeded + result reuse supported`는 allowlist 적용 뒤 `json.dumps(sort_keys=True, separators=(",", ":"), ensure_ascii=False, allow_nan=False).encode("utf-8")`와 동등한 canonical JSON 전체 크기가 최대 65,536 bytes인 `replay_result`를 저장하고 중복 전달에서 provider 재호출 없이 같은 node output schema를 반환해야 한다. JSON으로 표현할 수 없는 값, NaN/Infinity, projection 부재 또는 한도 초과는 값을 자르거나 문자열로 임의 변환하거나 raw payload를 저장하지 않고 최초 실행만 현재의 검증된 node output으로 계속하며 해당 attempt를 `result_unavailable`로 닫아야 한다. `succeeded + result reuse unavailable`도 최초 실행은 downstream 실행을 계속할 수 있지만 성공 사실만 영속 저장하고 `replay_result`는 저장하지 않아야 한다. 이후 중복 전달은 provider를 다시 호출하지 않고 non-retryable `external_effect.result_unavailable`로 종료해야 한다. 이 오류의 `code`, `retryable=false`와 optional `node_id`는 Node/application error부터 Workflow Engine, Celery task, Pub/Sub와 Gateway의 기존 실행 실패 응답·스트리밍 `error` 이벤트까지 문자열로 축소하지 않고 보존해야 한다. 기존 HTTP status와 error envelope은 유지해야 한다. Raw provider response/header/body를 저장하거나 오류에 넣고 replay 여부를 사용자-facing output에 임의로 추가하지 않아야 한다.
- FR-035 (MBA-190): 내부 identity, `effect_input_digest`, provider request identifier와 provider-visible raw key는 workflow input, template/Jinja variable map, LLM prompt/tool input, node output, 사용자 응답, external-effect 오류/metadata, durable trace, metric label과 일반 log에 노출하지 않아야 한다. 전체 URL/query/fragment/URL user info, raw request/response header/body, credential/token과 provider exception message 원문은 attempt, external-effect 오류/metadata, durable trace, metric label과 일반 log에 넣지 않아야 한다. 다만 Generic HTTP의 현재 실행이 기존 계약대로 반환하는 `status`, `data`, `headers` node output은 MBA-190에서 제거하거나 shape/값을 바꾸지 않고, 안전한 replay 결과로 영속 저장하지 않는다. 이는 PRD NFR-004의 기존 raw payload 응답 불일치를 해결하거나 새 노출을 허가하는 것이 아니며 별도 보안 계약 변경으로 남겨야 한다. Generic HTTP가 기존 trace metadata에 기록하던 목적지 `host`와 `path`는 유지하며 추가 비식별화하지 않는다. Key fingerprint는 operational attempt column에만 허용하고 node output, 사용자 응답, 오류, durable trace, 일반 log와 metric label에는 사용하지 않아야 한다. Trace와 attempt는 `workflow_run_id`/`node_run_id` correlation으로 연결해야 한다. Celery task 수신 INFO log와 `task-received|task-sent` event에도 실제 task args/kwargs를 내보내지 않아야 한다. 저장소 안에서 `app.send_task`를 호출하는 모든 `workflow.*` publisher는 공통 helper로 고정 redacted `argsrepr`/`kwargsrepr`을 설정해야 한다. Workflow task 전용 Task base는 `apply_async` 기본값을 같은 상수로 강제해 `.delay`, signature와 `self.retry` 발행을 보호하고, Request base는 publisher 값을 신뢰하지 않고 수신 log/event 발행 전에 다시 같은 값으로 덮어써야 한다. Repr에는 ID, graph/input, binding metadata와 secret-like 값이 없어야 한다.

FR-035 구현 세부 제약: Generic HTTP trace의 `host` 값은 URL user info가 포함되는 `netloc`이 아니라 parsed hostname과 명시 port만으로 만들고 `path` field 이름과 기존 경로 값은 유지해야 한다. Workflow Engine composition은 `httpx`, `httpcore`, `urllib3`/Requests transport logger의 raw record를 Worker에서 억제하고 adapter가 safe summary만 기록하게 해야 한다. 공통 `workflow.*` publish helper는 serialization/broker 오류를 raw exception message나 traceback으로 log/response에 넣지 않고 static operation과 exception class만 기록해야 하며 이 logger/publisher 정책을 Shared signal 또는 Log System Worker에 전역 적용해서는 안 된다.
- FR-036 (MBA-190): 1차 production contract에서 Generic HTTP mutation과 method에 무관한 `slackPostNode` request는 외부 서비스 중복 방지 `unknown`, GitHub `comment_pr`는 `unsupported`이고 세 operation의 결과 재사용은 `unavailable`이어야 한다. `provider/operation/contract_version`은 각각 `generic_http/generic_http.request/generic_http.request.v1`, `slack/slack.http.request/slack.http.request.v1`, `github/github.issue_comment.create/github.issue_comment.create.v1`으로 고정해야 한다. Generic HTTP와 Slack V1은 key 위치 `unknown`, 시스템 field `null`, key 형식·최대 길이·retention과 duplicate/conflict 의미 `unknown`으로 고정하고 시스템 key를 만들지 않아야 한다. GitHub V1은 key 위치 `none`, 시스템 field `null`, 형식·길이·retention 적용 안 함, duplicate-success 계약 없음으로 고정해야 한다. 세 profile 모두 replay projection이 없어야 한다. Generic HTTP `POST|PUT|PATCH|DELETE`, 모든 Slack request와 GitHub comment를 공통 external-effect 경계에 연결하고 Generic HTTP `GET`과 GitHub `get_pr`는 FR-047의 no-new-attempt read 경계를 유지해야 한다. 사용자 지정 URL, mode, method, header나 관찰된 동작으로 지원 수준을 올리거나 시스템 idempotency key를 생성·주입하거나 기존 header/body를 덮어써서는 안 된다. Generic HTTP runtime을 공유하는 Generic HTTP mutation과 Slack은 완전한 HTTP 응답을 status code와 관계없이 기존 node output으로 반환하고 `succeeded`로 닫으며 non-JSON body도 기존 text output으로 처리해야 한다. GitHub `201` 성공은 기존 `comment_id`, `comment_url`, `comment_body` node output shape와 값을 유지하되 replay result로 저장하지 않아야 한다. Local/transport/GitHub response 분류는 FR-031을, fake provider의 정확한 success/duplicate/retention/result 계약은 FR-040을 따라야 한다. Non-2xx 또는 Slack `ok=false`를 새 실패로 바꾸는 정책은 MBA-190 범위가 아니며 Slack provider-aware 판정은 MBA-218에 남겨야 한다. Fake support는 production composition이나 환경 설정에서 선택할 수 없고 production 지원 또는 exactly-once 증거로 사용해서는 안 된다. Gmail lifecycle/draft는 MBA-217 범위로 남겨야 한다.
- FR-037 (MBA-190): PostgreSQL migration은 table, stable effect slot unique/check constraint, correlation index, 모든 row의 provider contract readiness 일반 index와 재호출 가능한 supported row만 포함하는 HMAC readiness partial index를 만들고 SQLAlchemy model과 일치해야 한다. 별도 recovery scan index나 scheduler를 요구하지 않는다. 격리된 downgrade test가 해당 revision의 변경만 되돌려야 하고 migration 작성 시점의 단일 Alembic head를 parent로 사용해야 한다. Workflow Engine Worker만 시작 시 table/revision readiness, active Celery hard time limit의 존재·양수 여부와 terminal을 포함한 모든 attempt가 참조하는 과거 contract version을 검증해야 한다. Revision readiness는 기존 `apps/shared/services/alembic_readiness.py`로 현재 code head와 DB revision을 비교하고 필수 external-effect table/column/constraint/index shape를 함께 검사해야 한다. MBA-190 revision ID와 DB `alembic_version`의 정확한 일치만 요구해서는 안 되며 이후 descendant migration이 적용된 현재 head도 허용해야 한다. Production `supported` profile을 새 attempt에 사용할 수 있거나 재호출 가능한 supported attempt가 참조할 때만 versioned HMAC 설정을 필수로 검사한다. 필요한 값의 누락, 불일치 또는 잘못된 설정이면 provider 호출을 받기 전에 fail-closed해야 한다. 공용 Celery app, Log System과 다른 Worker는 MBA-190 readiness 때문에 시작을 거부해서는 안 된다.
- FR-038 (MBA-190): External effect 오류는 JSON-safe `{code, message, retryable, node_id?}` payload여야 한다. 최소 `external_effect.result_unavailable`, `external_effect.identity_conflict`, `external_effect.outcome_unknown`은 `retryable=false`여야 한다. Process 내부의 retry/terminal 제어 signal은 Loop `error_strategy=continue`, Node base와 WorkflowEngine generic exception catch보다 먼저 처리해 payload를 그대로 re-raise해야 하며 Loop 결과 문자열이나 workflow 성공으로 축소해서는 안 된다. WorkflowEngine은 control signal을 `str(e)` 기반 log/PubSub에 넣지 않고 safe run error만 기록한 뒤 task에 전달해야 한다. Stream error event는 task 경계에서 한 번만 발행해야 한다. Root task와 `WorkflowNode` nested engine의 cleanup 및 DB session close는 best-effort여야 하며 실패 시 exception type만 기록하고 원래 control signal, non-retryable safe result 또는 terminal commit 뒤 성공 output을 덮어쓰거나 generic retry를 시작해서는 안 된다. `WorkflowEngine.cleanup()`은 child 정리 실패와 무관하게 나머지 reference를 비우고 정리 예외를 호출자에게 전파하지 않아야 한다. Celery 경계에서는 custom exception serialization에 의존하지 않고 retry signal은 기존 `self.retry`, terminal error는 JSON-safe task result로 변환해야 한다. Node/application부터 Workflow Engine, Celery task result, Pub/Sub와 Gateway의 기존 스트리밍·비스트리밍 실패 경계를 통과할 때 payload를 보존해야 한다. Non-retryable task는 generic Celery retry를 호출하지 않아야 한다. 일반 동기 실행과 deployment run은 기존 HTTP status의 `detail`, stream은 `error` event, Compare variant는 기존 safe `error`와 additive `error_detail`, Cost Optimizer candidate는 기존 safe `error_message`와 additive `error_detail`에 mapping해야 한다. Webhook 접수 응답은 사후 실패로 변경하지 않고 caller가 사용하지 않는 task result에 payload를 보존해야 한다. Schedule claim schema/check에는 새 code를 추가하지 않고 `external_effect.outcome_unknown`은 기존 `execution_outcome_unknown`, `external_effect.result_unavailable`과 `external_effect.identity_conflict`는 기존 `execution_failed_after_admission`으로 finalization해야 한다. 구체 code는 task-local safe error와 허용된 trace에 두고 claim 또는 identity-conflict winner attempt row를 덮어쓰지 않아야 한다. Terminal attempt에 적용 가능한 allowlisted `error_code`는 저장할 수 있다. MBA-187 상태 전이와 retry를 바꾸지 않아야 한다. Digest나 provider 원문은 오류에 포함하지 않아야 한다.
- FR-039 (MBA-190): Provider port는 network-free `prepare_effect`, network-free `finalize_provider_call`과 network I/O를 수행하는 `invoke_effect`를 분리해야 한다. Application은 stable slot 조회로 기존 row의 frozen profile 또는 새 row의 active profile을 먼저 선택한 뒤 prepare를 호출해야 한다. Prepare 단계는 시스템 key를 넣기 전의 최종 application request와 `effect_input_digest`를 메모리에서 한 번 만들고 `PreparedEffectRequest` 또는 digest를 포함한 typed `failed_before_effect`를 반환해야 하며 URL/header/body/credential 원문을 저장하지 않아야 한다. Application은 prepare 결과와 candidate key metadata로 attempt를 claim해야 한다. 준비 실패면 provider-start marker 없이 `prepared -> terminal`로 닫아야 한다. 호출 가능한 claim owner는 candidate 값이 아니라 실제 winner row에 고정된 key version/format으로 key를 재생성해 fingerprint와 provider 길이를 검증하고, `finalize_provider_call`이 예약 위치에 시스템 key를 정확히 한 번 넣은 immutable `PreparedProviderCall`을 만든 뒤에만 `in_flight`를 commit해야 한다. `unsupported|unknown`은 사용자 request를 변경하지 않고 확정해야 한다. `invoke_effect`는 이 최종 call만 사용하고 graph/input/template을 다시 읽거나 header/body/key를 다시 조립해서는 안 된다. Finalize/fingerprint 실패는 `in_flight`와 provider 호출 전에 safe `failed_before_effect + stop`으로 닫아야 한다. Adapter bug, UTF-8 encode 실패 또는 안전한 canonical bytes를 만들 수 없는 크기/형식으로 digest 자체를 만들 수 없으면 digest/attempt를 꾸며내거나 기존 winner row를 변경하지 않고 provider/result/downstream 없이 non-retryable `external_effect.prepare_failed`로 종료해야 한다. 이 경로는 generic Celery retry를 호출하거나 raw 입력/exception message를 log해서는 안 된다. `invoke_effect`나 provider client를 prepare/finalize 단계에서 호출해서는 안 된다. Active profile이 바뀌어도 기존 attempt의 retry와 terminal duplicate delivery는 frozen profile로 digest를 계산해야 한다.
- FR-040 (MBA-190): Test-only `fake.create_effect.v1`은 최초 valid request에서 effect 하나를 만들고 `201`과 UUID 문자열 `effect_id`만 포함한 JSON을 반환해야 한다. 최초 수락부터 24시간 미만에는 같은 key/같은 canonical request를 새 effect 없이 `200`과 최초 `effect_id`로 반환하고, 같은 key/다른 request는 effect 없이 `409`로 거부해야 한다. Fake adapter가 `409`를 받으면 duplicate success로 바꾸지 않고 provider가 새 effect 부재를 확정한 `failed_before_effect + stop`과 allowlisted `provider_key_request_conflict`로 닫아야 한다. 정상 application 경로에서는 stored digest 불일치가 provider 전에 먼저 차단돼야 한다. 정확히 24시간 경계부터 같은 key는 새 effect와 `201`로 처리해야 하지만 Runtime은 attempt 생성 DB 시각 기준 `replay_deadline_at` 경계부터 자동 replay하지 않아야 한다. Result reuse `supported` projection과 최초/중복 node output은 `{"effect_id":"<uuid>"}` 하나로 같아야 하고 `unavailable` variant는 projection을 만들지 않아야 한다.
- FR-041 (MBA-190): `provider_started_at`은 effect 생성 여부, request byte 전송 여부나 Python 함수의 실제 진입을 증명하는 값이 아니라 최종 `PreparedProviderCall` 검증 뒤 `invoke_effect` 호출을 허용한 `in_flight` commit 시각이어야 한다. Commit 직후 함수 호출 전에 Worker가 종료돼도 non-null이어야 한다. `prepared`에서는 null, `in_flight`와 `terminal + succeeded|effect_outcome_unknown`에서는 non-null이어야 한다. `failed_before_effect`는 network-free prepare/finalize 단계의 local 실패면 null이고, `in_flight` commit 뒤 transport가 byte 미전송을 증명한 실패나 provider의 완전한 응답이 effect 부재를 확정한 rejection이면 non-null이어야 한다. 따라서 이 outcome은 두 nullability를 모두 허용해야 한다. `provider_status_code`와 allowlisted `error_code`는 terminal에서만 non-null일 수 있어야 한다. PostgreSQL CHECK와 domain validation이 이 구조를 함께 강제하고 `retry_before_effect|replay_same_key` reopen은 세 provider 결과 field를 모두 비워야 한다.
- FR-042 (MBA-190): Generic HTTP/Slack V1 header canonicalization은 현재 runtime의 application header merge 뒤 network 없이 `httpx.Headers(...).multi_items()`를 사용해야 한다. 렌더링된 body가 `None` 또는 빈 문자열이면 현재 `if body` 분기와 같은 no-body request로 보내고 사용자가 명시한 `Content-Type`을 보존해야 한다. 빈 문자열이 아닌 body를 JSON으로 parse한 결과가 `null`이면 HTTPX 0.28.1 `json=None`과 같이 사용자 `Content-Type`은 제거하지만 자동 `application/json`을 추가하지 않고 zero-byte body를 보내야 한다. 그 밖의 JSON 값은 사용자 `Content-Type` 변형을 case-insensitive하게 실제 outgoing header에서 제거하고 고정 `content-type: application/json`을 canonical input에 한 번 포함해야 한다. Digest용 JSON semantic form은 object key를 정렬해 key 순서만 다른 동등 JSON을 같은 값으로 보되, invoke는 원래 parsed value의 insertion order를 유지한 HTTPX 0.28.1 `ensure_ascii=False`, compact separator, NaN 금지 UTF-8 wire serialization을 사용해 기존 전송 bytes를 바꾸지 않아야 한다. 공백만 있는 body, NaN/Infinity와 JSON parse 실패는 rendered input을 포함한 deterministic `invalid_json`, `failed_before_effect + stop` 준비 결과여야 한다. Header 이름은 소문자로 묶어 이름순으로 정렬하되 같은 이름의 여러 값 순서는 보존하고, node가 명시하지 않은 나머지 HTTPX 기본 transport header는 digest에서 제외해야 한다. `prepare_effect`가 만든 key-free `PreparedEffectRequest`와 claim 뒤 시스템 key만 한 번 추가한 최종 `PreparedProviderCall`은 no-body/JSON-null-no-body/JSON mode, effective header와 parsed JSON 값을 공유해야 한다. Invoke는 최종 call을 유일한 request source로 사용하고 graph/input/template을 다시 렌더링하거나 header/body/key를 다시 조립해서는 안 된다. 이 wire semantics가 dependency 변경으로 달라지면 기존 contract를 수정하지 않고 새 version을 추가해야 한다.
- FR-043 (MBA-190): Provider replay `supported` profile의 예약 key header/body field가 사용자 요청에 이미 있으면 시스템 값을 덮어쓰거나 두 값을 함께 보내지 않고 network 전 allowlisted `provider_key_field_conflict`, `failed_before_effect + stop`으로 종료해야 한다. Header 충돌은 case-insensitive field name으로, body 충돌은 profile이 고정한 canonical JSON Pointer에 기존 값이 있는지로 판정해야 한다. `unsupported|unknown` profile은 시스템 key를 만들지 않고 사용자 field를 그대로 유지해 effect input digest에 포함해야 한다.
- FR-044 (MBA-190): Canonical framing V1은 domain tag와 순서가 고정된 field name/value UTF-8 bytes 각각에 unsigned 4-byte big-endian length를 붙여야 한다. UUID는 lowercase hyphenated 36자, 0 이상 정수는 leading zero 없는 base-10 ASCII여야 한다. Schedule execution ID와 node invocation ID는 ADR-0035의 UUIDv5 name 규칙을 사용해야 한다. Provider key format `hmac-b64url-v1`은 고정 field 순서의 V1 framing을 HMAC-SHA256한 padding 없는 Base64url이어야 하며, `idempotency_key_fingerprint`는 최종 provider-visible key ASCII bytes의 SHA-256 lowercase hex 64자여야 한다.
- FR-045 (MBA-190): 현재 gevent Worker에서 Celery soft time limit은 안전 경계로 사용하지 않아야 하고 configured hard time limit도 blocking task/process 종료의 증거로 사용하지 않아야 한다. Hard time limit은 claim TTL과 loser monotonic deadline의 bounded policy input으로만 사용하고, claim 만료 뒤 이전 network 요청이 살아 있을 수 있다는 전제에서 `supported` same-key provider 계약 또는 `unsupported|unknown` no-replay로 안전하게 닫아야 한다.
- FR-046 (MBA-190): Authenticated deployment와 URL slug API/public 실행은 Gateway가 발행하는 `workflow.execute` command에 `execution_id`, deployment ID/version과 canonical graph snapshot을 넣고 Worker가 DB deployment/App/Workflow provenance와 snapshot 일치를 재검증해야 한다. Webhook은 `workflow.execute_by_deployment` command에 `execution_id`와 deployment ID를 넣고 Worker가 canonical snapshot을 직접 조회해야 한다. Queue graph/provenance가 DB snapshot과 다르면 새 active deployment로 바꾸거나 owner로 fallback하지 않고 provider 호출 전에 fail-closed해야 한다.
- FR-047 (MBA-190): Generic HTTP `GET`과 GitHub `get_pr`는 새 external-effect attempt를 만들지 않아야 하지만, 같은 node invocation의 stable effect slot에 기존 row가 있는지는 read provider 호출 전에 확인해야 한다. 기존 row가 있으면 mutation/comment를 read로 바꾼 operation downgrade로 보고 read provider, 저장 결과와 downstream을 사용하지 않은 채 `external_effect.identity_conflict`로 종료해야 한다. 기존 row가 없을 때만 현재 read 경로를 그대로 실행해야 하며 read 성공 row를 새로 만들지 않아야 한다. `slackPostNode`는 logical side-effect node이므로 method와 무관하게 항상 `slack.http.request` effect 경계를 통과해야 한다.
- FR-048 (MBA-190): `WorkflowNode` target deployment는 parent retry나 duplicate delivery에서 현재 active deployment로 다시 선택해서는 안 된다. Gateway는 배포 생성 시 graph와 Loop subgraph의 각 WorkflowNode에 canonical target deployment ID/version/snapshot hash를 server-owned internal binding으로 계산해 immutable graph snapshot에 넣고, Draft/test·Compare·stream 최초 publisher도 각 surface가 기존 계약으로 선택한 검증된 실행 graph의 server-owned 복사본에 같은 binding을 계산해 queue command에 넣어야 한다. Binding 계산을 이유로 request graph를 DB draft로 대체하거나 저장해서는 안 된다. V1 metadata는 graph top-level `_nodease_runtime.workflow_node_bindings`의 `{version: "workflow-node-bindings.v1", entries: [...]}`이고 entry는 Loop `{kind,node_id}` `container_path`, `workflow_node_id`, `target_app_id`, `deployment_id`, `deployment_version`, `snapshot_sha256`만 가져야 한다. Entries는 path/node 순으로 정렬하고 중복을 거부해야 한다. Snapshot hash는 target의 전체 graph snapshot을 sorted-key/compact-separator/UTF-8/non-ASCII 유지/NaN 금지 canonical JSON으로 직렬화한 bytes의 SHA-256 lowercase hex 64자여야 하며 target의 자체 binding metadata도 포함해야 한다. Resolver는 기존 same-organization, allowed deployment type, cycle와 maximum nesting depth 정책을 재사용해야 한다. Binding use case는 `apps/gateway/application/deployment/`에 두고 기존 preflight와 같은 `DeploymentPreflightRepository` port, shared Loop-aware walker와 depth constant를 사용해야 한다. `apps/gateway/composition/deployment.py`가 기존 SQLAlchemy adapter를 조립하고 `DeploymentService`는 facade로 호출해야 하며, organization/type/cycle/depth DB 판정이나 새 resolver를 `apps/gateway/services/`에 복제해서는 안 된다. Client 입력뿐 아니라 deployment clone, template/import와 다른 graph 복사 경로의 internal metadata도 제거한 뒤 현재 canonical DB target으로 재계산해야 한다. 모든 client-facing draft/deployment/copy/export graph 응답, node output, trace와 log에는 metadata를 노출해서는 안 된다. Runtime은 binding을 canonical DB deployment/App/Workflow와 snapshot hash로 재검증하고 child graph의 binding을 이어 사용해야 한다. 기존 snapshot이나 legacy command에 binding이 없으면 canonical node catalog의 `side_effect=external_write`를 기본으로 한 shared pure classifier가 동적으로 선택한 child graph와 하위 closure에 effect-capable node가 없다고 확인한 경우에만 호환 실행해야 한다. `httpRequestNode GET`과 `githubNode get_pr`처럼 이 ADR이 명시한 read-only action만 낮출 수 있고, malformed/unknown action이나 새 `external_write` node는 안전하다고 추정해서는 안 된다. `gmailDraftNode`와 `mailAcknowledgeNode`도 binding 판정에서는 effect-capable이지만 실제 effect ledger는 계속 ADR-0032만 사용해야 한다. 하나라도 effect-capable이거나 분류할 수 없으면 child provider 호출 전에 fail-closed해야 한다. 이를 위해 새 admission table을 추가해서는 안 된다.

FR-048 기존 실행 계약 보존: Draft/test·Compare·stream publisher는 DB draft로 강제 통일하지 않고 각 surface가 MBA-190 이전부터 선택하던 검증된 실행 graph의 server-owned 복사본에 binding을 계산해야 한다. 특히 stream의 유효한 request `graph_snapshot`은 저장되지 않은 graph 그대로 실행 대상으로 유지하고 binding 계산을 이유로 DB draft로 대체하거나 저장해서는 안 된다. Shared pure classifier는 catalog 파일/loader를 import하지 않고 composition이 `apps/shared/services/workflow_node_catalog.py`에서 만든 immutable `node_type -> side_effect` mapping을 주입받아야 한다.
- FR-049 (MBA-190): Parent binding resolver는 immutable child deployment snapshot을 수정하거나 현재 active grandchild로 빈 binding을 보충해서는 안 된다. Child의 transitive closure에 FR-048 classifier가 effect-capable 또는 unknown으로 판정한 node가 있는데 child snapshot에 유효한 V1 binding이 없으면 parent 배포와 실행을 provider 전에 거부하고 child 재배포가 필요하다는 safe preflight error를 반환해야 한다. Closure에 effect-capable node가 없을 때만 legacy child snapshot 호환 경로를 허용해야 한다. Canonical node catalog의 모든 `external_write` entry는 MBA-190 common ledger profile, ADR-0032 별도 ledger owner 또는 명시적인 미구현 fail-closed 중 하나로 정적 검증돼야 하며 새 node가 조용히 호환 허용으로 빠져서는 안 된다.
- FR-050 (MBA-190): Generic HTTP `GET` 기존 read 경로도 FR-035의 trace/log 비노출 규칙을 적용해 raw request/response payload를 durable trace에 남겨서는 안 된다. 기존 `status/data/headers` node output과 safe `host`/`path`/method/status/size/latency metadata는 유지해야 한다.
- FR-051 (MBA-190): WorkflowNode binding 생성 시에는 target이 해당 app의 current active deployment이고 child surface에서 허용되는 type인지 확인해야 한다. Binding 소비 시에는 exact deployment row, app/workflow/organization provenance, 허용 type, version과 snapshot hash를 검증해야 한다. 새 child D2 활성화로 bound D1이 자동 비활성화된 경우에는 D1의 `is_active`나 app pointer와 D1의 일치를 요구하지 않고 기존 parent snapshot/command가 D1을 계속 사용해야 한다. 다만 app의 `active_deployment_id`가 null이거나 같은 app의 실제 active deployment row로 해석되지 않는 전체 비활성/불일치 상태는 kill switch로 보고 provider 전에 실패해야 하며 현재 pointer의 deployment로 bound graph를 대체해서는 안 된다. Bound row 삭제 또는 provenance/type/hash 불일치도 provider 전에 실패해야 한다.
- FR-052 (MBA-218): ADR-0037 적용 이후 `slackPostNode`는 FR-029/FR-036/FR-042/FR-047의 MBA-190 Slack 호환 baseline을 현재 실행 계약으로 사용하지 않고 전용 data/node/adapter를 사용해야 한다. API mode는 `slack.chat.post_message.v1`, Webhook mode는 `slack.incoming_webhook.post.v1`을 선택하며 두 profile의 provider replay는 `unknown`, safe result reuse는 `supported`여야 한다. 기존 `slack.http.request.v1`은 과거 attempt 해석용으로 등록 상태를 유지해야 한다. 성공 projection은 `status`, `delivery_status`, `delivery_mode`와 API mode의 검증된 `message_ref`만 허용하고 같은 stable slot 재진입은 provider 호출 없이 이를 재사용해야 한다. API 성공은 HTTP `200` JSON `ok=true`와 유효한 `ts`, Webhook 성공은 HTTP `200`과 정확한 `ok`를 모두 요구한다. 확인된 rejection과 `429`는 `failed_before_effect + stop`, partial effect 가능 오류·미확인 `ok=false`·malformed success·redirect·write/read timeout·응답 유실·`5xx`는 `effect_outcome_unknown + stop`이어야 하며 `Retry-After`를 포함한 어떤 Slack provider 실패도 자동 replay하지 않아야 한다. Template은 실제 사용된 등록 변수의 단순 치환만 허용하고 JSON template 값은 구조를 바꾸지 못하게 escape해야 한다. API endpoint는 고정하고 Webhook은 exact commercial URL만 허용하며 arbitrary method/header/body/timeout을 request source로 사용하지 않아야 한다. 제거된 `data`/`headers` 및 Webhook `message_ref` selector는 Client/deployment/runtime에서 fail-closed하고, token/webhook URL/message/raw response와 `message_ref` 원문은 public graph/log/trace에 저장하지 않아야 한다.
- FR-053 (MBA-233): LLM node data는 `knowledgeBases`와 additive `knowledgeCollections`를 동시에 지원하고 각 목록을 최대 20개로 strict validation해야 한다. 구 graph에서 Collection field 부재는 빈 목록이며 over-limit reference를 slice하거나 unknown reference field를 무시해서는 안 된다.
- FR-054 (MBA-233): Workflow Builder는 고정 Knowledge Base와 실행 시 membership을 해석하는 Knowledge Collection을 별도 그룹과 독립 `n/20` count로 표시해야 한다. Collection 관리/권한/child 편집은 Builder가 아니라 Knowledge 관리 화면의 책임이며 stale selection은 generic unavailable로 표시하고 자동 삭제하지 않아야 한다.
- FR-055 (MBA-233): Draft save, Agent Builder apply, optimizer/model-routing graph mutation은 server-side structural validation과 current editor의 direct KB `use`/Collection `route` 재검증을 우회하지 않아야 한다. 저장된 label과 Client capability는 권한이 아니며 실패는 partial graph 또는 success audit을 만들지 않아야 한다.
- FR-056 (MBA-233): Deployment preview/create/activation/toggle은 direct KB와 selected Collection을 server-derived audience로 preflight하고 private/anonymous 또는 source-public-exposure 차단을 safe reason/action과 bucket으로만 반환해야 한다. Preflight 성공은 runtime authorization capability가 아니다.
- FR-057 (MBA-233): 모든 production Workflow execution surface와 nested Loop/WorkflowNode는 같은 MBA-232 resolver dependency를 LLM node에 전달해야 한다. Knowledge configuration이 있으면 invocation마다 resolver를 한 번 호출하고 direct/Collection duplicate KB를 한 번만 retrieval해야 한다.
- FR-058 (MBA-233): Resolver policy zero-result 또는 insufficient evidence는 LLM provider를 호출하지 않아야 한다. Resolver infrastructure failure는 raw 원인 없는 retryable workflow failure로 Celery retry 경계에 전달하고, authorized candidate resolution 이후 일부 retrieval failure만 기존 partial-result/evidence policy를 적용해야 한다.
- FR-059 (MBA-233): Agent Builder, cost optimizer, compare, copy/import, model routing policy와 deployment snapshot은 operation이 Collection selection을 명시적으로 변경하지 않는 한 `knowledgeCollections`를 round-trip해야 한다. Public graph와 durable trace/log projection은 두 Knowledge reference 목록과 Collection identity를 제거해야 한다.
- FR-060 (MBA-233): Pre-execution Knowledge sync는 explicit `knowledgeBases`만 대상으로 하고 Collection membership을 resolver 전에 확장하거나 live connector를 호출하지 않아야 한다. Source-managed Collection child는 MBA-232의 materialized provenance/readiness gate를 따른다.
- FR-061 (MBA-233): 새 Collection graph는 모든 Worker가 MBA-233를 실행하고 기존 task를 drain한 뒤에만 Gateway/Client에서 저장 가능해야 한다. 자동 Worker capability negotiation이 없는 현재 구현은 Worker-first 배포 절차와 역순 rollback evidence를 요구한다.
- FR-062 (MBA-219): Agent Builder preview와 Workflow draft 저장은 unresolved Mail/Gmail Draft/Mail Acknowledge/Slack node를 보존할 수 있지만 secret 저장 금지, organization scope와 non-null reference permission 같은 기존 storage boundary는 완화하지 않아야 한다.
- FR-063 (MBA-219): Authenticated draft graph publisher인 `/execute`, `/stream`, Compare variant는 workflow execute 권한, Cost Optimizer compare/recommendation verification candidate는 기존 write/builder 권한과 active organization 검증 뒤 Celery task publish 전에 같은 configuration preflight를 current user principal로 수행해야 한다. 하나라도 blocked면 해당 task를 발행하지 않아야 한다. 완료된 recommendation verification의 idempotent safe response replay는 새 publisher가 아니므로 workflow 권한과 active organization scope만 다시 확인하고 현재 node/graph provider/preflight를 다시 시작하지 않는다.
- FR-064 (MBA-219): Interactive execution 차단은 HTTP `409`와 `workflow.configuration_preflight.blocked` error code, safe reason/action과 기존 preflight response projection을 반환해야 한다. Stream은 SSE/Redis subscribe를 시작하기 전에 같은 JSON error를 반환해야 한다.
- FR-065 (MBA-219): Managed preflight는 최상위 graph와 모든 Loop `subGraph`, WorkflowNode target에서 Mail credential lifecycle/use, Gmail OAuth/processing selector/path와 Slack 전용 graph readiness를 검사해야 한다. Parent graph의 audience와 principal을 child target에도 전달하고 server-owned binding이 있으면 exact deployment/version/snapshot hash의 child를 검사해야 한다.
- FR-066 (MBA-219): Missing/revoked/cross-organization/permission-denied Mail credential은 API에서 모두 `mail_credential_unavailable`로 정규화하고 credential identity, mailbox email, token, Webhook URL, channel, message/body와 raw exception을 노출하지 않아야 한다.
- FR-067 (MBA-219): LLM, HTTP와 GitHub는 기존 runtime-authoritative 권한/egress 경계를 유지하고 registry에 명시한다. External side effect node가 catalog에 추가됐지만 registry 정책이 없거나 `implemented=false`이면 test/active/schedule 실행 전에 fail-closed해야 한다.
- FR-068 (MBA-219): Configuration preflight는 provider에 연결하거나 secret을 복호화하지 않아야 한다. Mail/Slack/LLM/HTTP/GitHub runtime은 preflight 통과 뒤에도 resource scope/status/use, secret, egress와 provider 계약을 다시 검증해야 한다.
- FR-069 (MBA-219): Shared pure graph validator는 최상위와 Loop subgraph에서 node/edge shape, duplicate node ID, dangling endpoint, cycle, surface별 진입점과 isolation을 검사해야 한다. 모든 node는 유한한 JSON number `x`, `y`를 가진 `position`을, 모든 edge는 비어 있지 않은 string `id`를 가져야 한다. 최상위 graph는 명시적 trigger/start node가 정확히 하나여야 한다. Loop body는 별도 trigger를 요구하지 않고 incoming executable edge가 없는 실행 node가 정확히 하나여야 하며, 모든 실행 node가 그 진입점에서 도달 가능해야 한다. 최상위와 모든 subgraph 합산 상한은 node 1,000개와 edge 5,000개이며 Loop subgraph 중첩 깊이는 16이다. `/execute`, `/stream`, Compare/Cost Optimizer, deployment preflight와 Loop runtime은 같은 structural contract를 WorkflowNode target resource lookup, task publish 또는 body 실행 전에 적용해야 한다. Compare와 Cost Optimizer는 preflight를 통과한 server-bound graph 복사본에서 candidate를 만들고 dispatch 전 current target으로 다시 binding해서는 안 된다.
- FR-070 (MBA-219): Mail managed validator는 Worker Mail schema와 같은 공통 `title`, folder, `max_results`, boolean, filter/date/reference, selector와 processing mode 타입·범위를 검사해야 한다. `credential_id=null`은 `configuration_state=unresolved`인 draft/inactive 편집 상태에서만 보존할 수 있다. 단, `configuration_state` 도입 전 Client가 저장한 `credential_id=null` node에서 그 필드가 아예 없으면 legacy unresolved와 동일하게 취급하되 명시적 null/그 밖의 값은 invalid로 유지한다. Non-null reference는 deployment 저장 시 scope/status/use가 유효해야 한다.
- FR-071 (MBA-219): Pure preview는 durable permission audit을 만들지 않아야 한다. Active/inactive deployment create, activation과 authenticated test/stream/Compare/Cost Optimizer enforcement가 same-organization active Mail credential의 `use` 거부를 확인하면 resource hiding 응답을 유지하면서 요청당 resource별 `permission.denied`를 정확히 한 번 기록해야 한다.
- FR-072 (MBA-219): Mail preflight permission은 organization/user/membership을 요청당 한 번 판정하고 direct/team grant를 active same-organization credential 집합으로 조회해야 하며 기존 scalar effective auth-state와 같은 결과를 반환해야 한다. Revoked credential은 scalar/bulk 어느 경로에서도 잔존 grant를 운영 권한으로 집계하지 않는다.
- FR-073: 테스트 실행 stream은 시작 직후 safe `run_id`를 전달해야 한다. Client는 이 식별자와 optional 선택 `node_id`만 URL에 보관하고, 같은 workflow의 실행 상세 API가 read 권한을 다시 확인한 뒤 반환한 기록으로 TestSidebar를 복원해야 한다. 원본 입력·출력·credential·trace 원문을 브라우저 저장소나 URL에 저장해서는 안 된다. 같은 workflow를 다시 동기화하는 과정은 테스트 실행 상태를 초기화하지 않지만, 다른 workflow 전환과 사용자의 `다시 테스트하기`는 기존 테스트 상태를 초기화한다.
- FR-074 (MBA-270): 빌더는 workflow 편집 화면과 TestSidebar를 벗어나지 않고 저장된 실행 하나를 명시적으로 기준 실행으로 고정한 뒤 현재 테스트 실행과 비교해야 한다. 비교 모드는 최신 실행을 자동 선택하지 않으며, 기준 실행은 사용자가 변경하거나 비교 모드를 종료하기 전까지 재실행에도 유지된다. 노드 목록 비교는 성공 여부, 비용, 실행 시간, 토큰만 표시하고 `노드 상세 비교하기`에서 같은 사이드바 안에 양쪽 입력·출력과 LLM 모델 라우팅 근거를 표시한다. Test/stream 실행의 라우팅 결과는 활성 배포 정책을 미리 보는 용도이며 정책 학습 표본에는 포함하지 않는다.
- FR-075 (Workflow Draft CAS And Agent Builder GraphMutation Save): 일반 autosync를 포함한 모든 `POST /workflows/{workflow_id}/draft`와 graph를 변경하는 Model Routing/Cost Optimizer apply API는 expected graph hash와 workflow `updated_at`을 포함해야 한다. Gateway는 workflow row를 write lock으로 조회하고 권한을 재확인한 뒤 두 기대값을 current canonical 값과 비교해야 하며, 먼저 저장된 변경이 있으면 늦은 요청을 `409 stale_graph`로 닫아 silent overwrite, 자동 merge와 강제 덮어쓰기를 막아야 한다. Frontend는 Agent Builder, autosync, version 복원, test 전 저장, Model Routing, Cost Optimizer와 Undo/Redo가 공유하는 canonical metadata 상태 하나를 성공한 graph read/write 결과로 갱신해야 한다. Agent Builder mutation은 operation id, action, catalog version과 persisted safe envelope의 `expected_result_graph_hash` 검증을 이 공통 CAS 위에 추가한다. Full typed operations는 DB에 저장하거나 재생하지 않는다. Graph write와 같은 transaction의 policy/candidate/audit 변경 중 하나라도 실패하면 전체를 rollback해야 한다. Draft save와 canonical draft read 및 out-of-band graph-write 응답은 모두 server-calculated graph hash, DB `updated_at`, workflow id를 반환하고 별도 workflow version/revision column을 합성하지 않는다.
- FR-075a (Test Preflight Save Coordination): 같은 browser editor의 Agent Builder save/acknowledgement, test preflight save, autosync, Undo/Redo와 version restore는 workflow별 save coordinator로 직렬화한다. Test preflight가 canonical graph를 검증·저장한 뒤 실행을 시작하려는 시점에 owner 종류와 무관하게 대기 중인 저장이 하나라도 있으면 stream을 열지 않고 lock을 해제해 대기 저장을 먼저 처리하며, 저장 확정 뒤 사용자가 다시 실행하게 한다. 대기 저장이 없으면 valid `workflow_start.run_id`로 서버 실행 snapshot이 확정될 때까지 `test_preflight` lock을 유지하고 그 사이 도착한 저장을 대기시킨다. 실행 시작 실패·취소·timeout에서는 lock을 해제하며, 실행 전체가 끝날 때까지 lock을 유지하거나 test를 자동 재실행하지 않는다. Lock 때문에 건너뛴 autosync는 workflow별 최신 dirty snapshot 하나로 합쳐 lock 해제 뒤 한 번 저장하고, workflow 전환·clean 전환·Agent Builder 저장 상태면 중단한다. 충돌한 test preflight는 typed operation을 재생하거나 graph를 자동 덮어쓰지 않는다. `409 stale_graph` 복구는 canonical graph가 local snapshot과 같은지 먼저 비교하고 같을 때만 최신 hash/`updated_at`을 수용한다. `409 operation envelope not found`는 session safe envelope와 canonical draft로 `applied|unapplied|pending|stale`을 판정하며 acknowledged result hash와 saved workflow `updated_at`이 canonical 값과 모두 같은 `applied`에서만 실행을 계속한다. `401`, `403`, 두 `409`와 일반 저장 실패를 구분한다.
- FR-075b (Version Restore Workflow Identity): version 복원은 시작 시점의 active workflow id로 workflow별 save lock을 기다리고, lock 획득 직후 active workflow id가 여전히 같은지 다시 확인해야 한다. 대기 중 사용자가 다른 workflow로 전환했다면 canonical draft 조회와 저장을 시작하지 않고 editor graph, canonical metadata와 Undo/Redo history를 변경하지 않은 채 복원을 종료한다.
- FR-075c (Agent Builder Save Workflow Identity): Agent Builder mutation 저장은 lock 획득 직후와 canonical draft 조회 직후 시작 workflow가 여전히 active인지 확인해야 한다. 다르면 mutation 적용, draft save, acknowledgement와 rollback을 모두 중단하고 새 workflow의 graph, metadata와 history를 변경하지 않은 채 안전한 전환 안내를 표시한다.
- FR-075b (Locked Permission Recheck And Canonical Feature Validation): 모든 draft save는 endpoint 진입 검사와 별개로 workflow row write lock 뒤 active organization/write 권한을 다시 확인한다. 권한이 회수되면 graph, features, task 상태와 audit을 변경하지 않는다. Agent Builder와 일반 save는 같은 recursive canonical projection을 graph, 중첩 `subGraph.nodes`/`subGraph.edges`와 `features.noteNodes`에 적용한다. 잘못된 `features.noteNodes` 또는 중첩 node/edge schema는 DB를 변경하지 않고 safe `422 workflow.features_invalid|workflow.graph_invalid`로 거부하며 raw validation detail을 노출하지 않는다.
- FR-076 (Unresolved External Action Preflight): Gateway는 Catalog required configuration 전체에서 node `configuration_state`를 다시 계산해야 한다. 하나라도 missing/deferred/invalid인 Slack, GitHub, HTTP, Mail 등 외부 action node가 있는 workflow는 편집·draft 저장할 수 있지만 server-side test/run과 deployment preflight에서 차단해야 한다. Preflight는 저장 graph와 catalog만 검사하며 client 상태, credential provider 또는 외부 API를 신뢰·호출하지 않는다.
- FR-077 (Agent Builder Workflow History Boundary): 최초 acknowledged `initial_graph`/`graph_edit`/`replace_workflow` operation은 Agent Builder 실행 전 graph와 최신 canonical final graph를 하나의 Workflow history boundary로 관리해야 한다. 후속 `parameter_update`/`knowledge_binding`은 별도 Workflow history entry를 만들지 않고 boundary의 final graph hash와 `updated_at`만 갱신한다. 완료 상태의 첫 Undo는 graph, parameter 값과 persisted task 상태를 유지한 채 `completed|skipped|deferred` 중 최대 stable order의 재편집 가능 ParameterTask UI에 client presentation으로 재진입하고, parameter UI가 열린 상태의 다음 Undo 또는 task가 없는 완료 상태의 첫 Undo는 `action=revert` CAS로 실행 전 graph 전체를 복구한다. 이 transaction은 생성 node/edge, parameter 값과 KB binding을 제거하고 모든 ParameterTask/Knowledge 흐름을 `canceled`로 닫는다. `parameter_update`/`knowledge_binding` operation id의 개별 persisted revert는 거부한다. 전체 복구 뒤 reload 전 `action=redo` CAS는 final graph만 복구하고 canceled task/Knowledge 흐름을 재실행하지 않으며, 그 뒤 다시 Undo하면 parameter 재진입 없이 같은 boundary를 즉시 revert한다. 동일 revert/redo context 재시도는 중복 write/audit 없이 같은 canonical 성공을 반환한다. Redo history와 parameter 재진입 상태는 client memory에만 유지하며 reload 뒤 복구하지 않는다.
- FR-078 (MBA-283): Generic HTTP node/provider는 HTTP client 또는 socket을 직접 생성하지 않고 Workflow Engine application outbound HTTP port만 사용해야 한다. Composition은 공통 `OutboundEgressGuard`와 concrete HTTPX adapter를 조립하며 production node/provider의 직접 client 생성은 architecture contract test로 차단해야 한다.
- FR-079 (MBA-283): Generic HTTP outbound는 HTTP/HTTPS, `GET|POST|PUT|PATCH|DELETE`와 public 80/443만 허용해야 한다. URL userinfo/fragment, hop-by-hop/proxy 제어 header와 환경 proxy를 거부하고 loopback, private, link-local, multicast, reserved, unspecified, carrier-grade NAT와 metadata IPv4/IPv6 목적지를 fail-closed해야 한다. DNS 결과 중 하나라도 차단 대상이면 전체 요청을 거부해야 한다.
- FR-080 (MBA-283): Generic HTTP transport는 검증된 전체 IP 목록을 DNS/OS 순서대로 실제 TCP destination 후보로 사용하고, request byte가 전송되지 않은 TCP connect 실패에만 다음 검증 IP를 시도해야 한다. 연결 peer가 현재 선택한 IP와 일치하는지 요청 byte 전송 전에 확인하고 정책 거부나 peer mismatch에서는 폴백하지 않아야 한다. HTTPS SNI와 certificate hostname 검증에는 원래 hostname을 유지해야 하며 `verify=false`나 request-controlled CA를 제공해서는 안 된다. Redirect와 environment proxy는 사용하지 않아야 한다.
- FR-081 (MBA-283): Generic HTTP egress는 header/request body/timeout/response body 상한과 identity-only response encoding을 적용해야 한다. Policy 거부는 raw URL/query/header/body/IP를 노출하지 않는 `invalid_prepared_request`, 전송 전 일시 DNS/connect 실패는 기존 `connection_failed`, 요청 뒤 response 검증/read 실패는 outcome-unknown 계약으로 닫아야 한다. 정상 HTTPX V1 serialization, status/data/headers output과 status-agnostic 성공 판정은 유지해야 한다.
- FR-082 (MBA-283, MBA-357에서 network path 대체): Provider-neutral Helm의 Worker는 DNS, configured PostgreSQL port, Redis, Sandbox와 internal Squid만 직접 허용하고 public 80/143/443/993 direct route는 허용하지 않아야 한다. Public HTTP/HTTPS와 address-pinned IMAP tunnel은 FR-088의 internal Squid listener만 사용한다. 외부 dependency CIDR은 해당 configured service port에만 적용하고 public catch-all, link-local과 metadata CIDR을 거부해야 한다.
- FR-083 (MBA-285): 하나의 logical Workflow 실행에서는 root engine만 부모 `WorkflowRun` 생성·완료·실패와 최상위 Redis lifecycle event를 소유해야 한다. Loop body와 WorkflowNode target child는 부모 `execution_id`와 run correlation을 유지하고 iteration/target별 invocation path를 사용하되 부모 run terminal 상태, 최상위 workflow/node event와 별도 child run/node log를 만들지 않아야 한다. Child 성공 결과는 container node가 수집하고, 일반 오류는 기존 Loop `error_strategy`를 따라야 한다. Timeout은 부모 실패 경계로 전파하고 external-effect control signal은 payload를 축소하지 않고 부모에 전달해야 한다. Root는 전체 graph outcome을 정확히 한 번 기록해야 하며 모든 child 생성 경로는 이 lifecycle 정책을 강제하는 공통 factory를 사용해야 한다.
- FR-084 (MBA-286): Workflow scheduler는 selector data dependency와 실행 중 활성화된 incoming control edge를 별도로 추적해야 한다. 명시적 entry를 제외한 node는 모든 incoming edge가 active 또는 inactive로 확정되고, active predecessor와 모든 selector source가 성공한 뒤에만 한 번 enqueue해야 한다. Condition의 선택되지 않은 case/default edge와 해당 경로는 inactive로 전파해 join을 막지 않아야 하며, inactive selector source를 요구하는 node는 누락된 입력으로 실행해서는 안 된다. Control edge 확정과 selector source 완료는 모두 영향받는 target readiness를 다시 평가해야 한다. 실패·timeout·cancel은 기존 fail-fast 경계를 유지하고 downstream을 시작하지 않아야 한다. 이 scheduler 상태는 `RuntimeDataDependencyEnvelope` provenance와 별도 실행별 메모리 상태이며 DB/API schema를 추가하지 않는다.
- FR-085 (MBA-352): Root와 Loop `subGraph` node의 canonical graph identity는 ordered Loop-only `container_path + node_id`다. Node/parent ID는 최대 255자, path depth는 16이며 Shared pure walker가 Gateway graph preflight, WorkflowNode binding, deployment LLM credential policy와 Workflow Runtime의 location 판정을 통일해야 한다. 서로 다른 container의 동일 `node_id`는 허용하지만 같은 container의 duplicate, unknown kind, stale/missing path 또는 structured path/digest 불일치는 provider 외부 I/O 전에 fail-closed해야 한다. Runtime은 client graph/input의 path가 아니라 trusted child execution control의 path를 사용한다.
- FR-086 (MBA-178): Workflow FileExtraction의 원격 파일 입력은 node가 직접 HTTP client를 만들지 않고 composition에서 주입한 remote-file application port를 사용해야 한다. Adapter는 HTTPS/443, public address-pinned TLS, response/content type 상한과 bounded same-origin redirect를 강제하고 raw URL/path/exception 없는 typed failure만 node에 반환해야 한다. Response body는 전체를 memory에 적재하지 않고 byte 상한을 확인하며 임시 파일로 직접 streaming하고 실패한 partial file을 제거해야 한다.
- FR-087 (MBA-178): Workflow LLM generation·embedding과 model discovery는 현재 server-owned Provider endpoint에 binding된 operation profile과 guarded transport를 사용해야 한다. Stored credential의 과거 endpoint snapshot, ambient proxy, redirect 또는 provider SDK default transport가 current endpoint·policy를 덮어쓸 수 없어야 한다.
- FR-088 (MBA-357): Production Workflow public HTTP/HTTPS는 application guard 뒤 server-owned Squid `3129` listener를 명시적으로 사용해야 한다. HTTPS는 CONNECT 443, Generic HTTP만 public 80을 사용할 수 있고 proxy failure 뒤 direct transport로 fallback하거나 ambient proxy/`NO_PROXY`를 사용해서는 안 된다. Mail IMAP, DB, Redis와 Sandbox control은 이 listener를 사용하지 않는다.
- FR-089 (MBA-357): Worker NetworkPolicy의 `canary` phase는 `nodease.io/egress-mode=proxy-v1` revision만, `final` phase는 모든 Worker pod를 선택해야 한다. Pinned Calico CI에서 direct public HTTPS 실패와 proxy 경유 성공을 실제 검증하고, additive allow policy 또는 NetworkPolicy 미집행 CNI가 있는 환경은 activation을 거부해야 한다.


### 1. 실행 편의성

- 테스트 실행은 기존 workflow 실행 경로와 스트리밍 이벤트를 사용한다.
- `node_start` 수신 시 해당 노드는 실행 중 상태로 표시된다.
- `node_finish` 수신 시 해당 노드는 성공 상태, 소요 시간, 토큰 사용량을 표시한다.
- 실행 오류가 노드에 연결된 경우 해당 노드는 실패 상태와 소요 시간을 표시한다.
- 노드별 실행 요약은 테스트 실행 사이드바 안에서 확인할 수 있어야 한다.
- 노드별 실행 요약은 노드명, 실행 상태, 소요 시간, 비용, 토큰 사용량을 포함해야 한다.
- 백엔드 스트리밍 `node_finish` 이벤트는 노드별 `latency_ms`, `total_tokens`, `total_cost` 표준 필드를 제공해야 한다.
- 프론트는 노드별 소요 시간, 비용, 토큰 사용량을 표시할 때 `node_finish`의 표준 필드를 우선 사용해야 한다.
- 워크플로우 테스트가 완료되면 테스트 실행 사이드바의 마지막 영역에 서버 실행 시간, 화면 완료 시간, 전체 비용, 전체 토큰 사용량을 최종 요약으로 표시해야 한다.
- 워크플로우 테스트가 성공하면 테스트 실행 사이드바의 성공 결과 상단에 `최종 응답` 카드를 표시해야 한다.
- 최종 응답 카드는 `workflow_finish.output` 또는 response/answer node 최종 output을 우선 사용하고, 없으면 LLM node output의 `text`, `message`, `answer`, `content` 계열 값을 fallback으로 사용한다.
- 최종 응답이 JSON object이면 raw JSON dump만 노출하지 않고 읽기 쉬운 field preview를 표시해야 한다. 원본 노드별 JSON output은 기존 노드별 실행 결과 상세 영역에 유지한다.
- 서버 실행 시간은 백엔드/엔진이 기록한 workflow run의 실행 시간이다. 가능한 경우 `workflow_runs.duration` 또는 stream 완료 이벤트가 제공하는 workflow-level duration을 사용한다.
- 화면 완료 시간은 프론트가 테스트 실행 시작 상태로 전환된 시각부터 성공/실패 완료 상태로 전환된 시각까지 계산한 시간이다. 이 값에는 네트워크, stream 처리, UI 상태 갱신, 시각적 지연이 포함될 수 있다.
- 최종 요약에서는 서버 실행 시간을 주 지표로, 화면 완료 시간을 보조 지표로 표시해야 한다.
- 테스트 실행 사이드바 요약은 A/B 테스트 구현을 대체하지 않는다. 단일 테스트 실행 결과를 명확히 확인하는 UI이며, 이후 variant 비교 UI의 기반 정보로 사용할 수 있어야 한다.
- 테스트 실행 사이드바는 오른쪽에 고정하고, 왼쪽 경계를 드래그해 가로 폭을 조정할 수 있어야 한다.
- 기본 폭은 `560px`, 최소 폭은 `440px`, 최대 폭은 `720px`이다. 화면 폭이 충분하지 않으면 화면 안에 표시 가능한 폭을 우선하고 리사이즈 조작은 숨긴다. 패널 안의 글자는 기존 크기보다 한 단계 크게 표시한다.
- 사용자가 조정한 테스트 실행 사이드바 폭은 같은 편집 세션에서 패널을 닫고 다시 열어도 유지한다. 새로고침 이후 영구 저장은 이번 범위가 아니다.

### 2. 노드 조작 편의성

- 노드 상세 편집 화면은 3패널 구조를 유지한다.
- 사용자는 패널 사이의 resizer를 드래그해 좌우 가로 비율을 조정할 수 있어야 한다.
- 각 패널에는 최소/최대 가로 폭이 있어야 하며, 전체 화면을 벗어나거나 특정 패널이 사용 불가능할 정도로 줄어들면 안 된다.
- 맨 왼쪽 패널을 넓히면 가운데와 오른쪽 패널이 비슷한 비율로 공간을 내주어야 한다.
- 가운데 또는 오른쪽 패널을 넓히는 경우에도 나머지 패널의 최소 폭과 전체 최대 폭 제약을 지켜야 한다.
- 노드 상세 편집 영역은 화면 가로 폭의 최대 90%까지 사용할 수 있어야 하며, 고정 픽셀 최대 폭에 묶이지 않아야 한다.
- 사용자가 패널 폭을 직접 조정하기 전의 기본 레이아웃은 부모 영역 기준 최대 90% 폭을 사용하고, 왼쪽/가운데/오른쪽 패널을 비율 기반으로 배치해야 한다.
- 기본 패널 비율은 왼쪽 28%, 가운데 52%, 오른쪽 20%를 기준으로 한다. 단, 각 패널의 최소/최대 폭 제약이 이 비율보다 우선한다.
- 패널 가로 비율은 현재 편집 세션 안에서 유지되어야 한다. 영구 저장은 이번 범위가 아니다.

### 3. 워크플로우 조작 편의성

- 사용자가 캔버스에서 노드를 선택한 상태로 왼쪽 노드 패널의 노드 항목을 클릭하면, 기본 후보 동작은 선택된 노드 뒤에 새 노드를 추가하는 것이다.
- 왼쪽 노드 패널의 노드 항목은 일반 추가와 `뒤에 추가` 액션을 구분해서 제공해야 한다. 일반 추가는 기존 자유 배치 또는 캔버스 추가 흐름을 유지하고, `뒤에 추가`는 선택된 노드 오른쪽에 새 노드를 배치한 뒤 edge를 자동 생성한다.
- 선택된 노드가 없고 graph에 terminal node가 하나뿐이면 `뒤에 추가`는 해당 terminal node 뒤에 새 노드를 연결할 수 있다. terminal node가 여러 개면 임의로 마지막 노드를 고르지 않고 액션을 비활성화하거나 연결 대상 선택 UI를 요구한다.
- terminal node 개수 계산에서는 sticky note처럼 workflow 실행 graph에 포함되지 않는 보조 노드를 제외한다.
- 새 노드 자동 배치는 전체 graph를 재정렬하지 않고, 기준 노드 오른쪽의 local placement만 수행해야 한다. 사용자가 이미 만든 배치를 망가뜨리면 안 된다.
- condition, switch, loop처럼 여러 handle 또는 분기 의미가 있는 노드 뒤에 추가할 때는 연결할 handle이 명확한 경우에만 자동 연결한다. 애매하면 handle 선택 UI를 요구하거나 `뒤에 추가`를 제한한다.
- 사용자는 캔버스에서 노드를 선택한 뒤 Backspace 또는 Delete 키로 해당 노드를 삭제할 수 있어야 한다.
- 삭제 대상 노드에 들어오는 edge와 나가는 edge가 모두 있으면, 삭제 후 가능한 경우 upstream 노드와 downstream 노드를 자동으로 다시 연결해야 한다.
- 자동 재연결은 기존 edge 방향을 보존한다. 삭제된 노드의 upstream source는 downstream target으로 연결된다.
- 자동 재연결은 유효한 연결 규칙을 통과하는 경우에만 수행한다. 노드 타입, handle, 그래프 검증 규칙상 연결할 수 없으면 재연결하지 않고 삭제만 수행한다.
- 삭제 대상 노드가 여러 upstream 또는 여러 downstream edge를 가진 경우에는 가능한 모든 유효한 upstream/downstream 조합을 재연결하되, 중복 edge는 만들지 않는다.
- 여러 노드를 동시에 삭제하는 경우에는 삭제 후 남는 노드들 사이에서만 유효한 자동 재연결을 계산해야 한다.

### 4. 노드 실행 기록 패널 추가

- 노드 상세 패널에는 `실행 기록` 탭을 제공한다.
- `실행 기록` 탭의 기본 상태에는 `실행 목록 검색`과 `가장 최신 로그 기록 불러오기` 액션을 제공한다.
- `실행 목록 검색`을 누르면 오른쪽 패널 전체가 실행 로그 선택 화면으로 전환되어야 한다.
- 실행 로그 선택 화면은 워크플로우 실행 로그를 최신순으로 표시하고, 상태/검색어 기반 필터를 제공해야 한다.
- 실행 로그 row는 전체 workflow run 요약과 현재 선택된 node_id의 노드 실행 기록 preview를 함께 표시해야 한다.
- 사용자가 실행 로그 row를 선택하면 패널은 다시 `실행 기록` 탭 상세 상태로 돌아와 선택한 실행 로그 안의 현재 노드 input/output, 상태, 소요 시간, 토큰, 비용, 오류 정보를 표시해야 한다.
- `가장 최신 로그 기록 불러오기`는 현재 workflow의 실행 로그 중 현재 node_id 기록이 존재하는 가장 최신 실행 기록을 불러와야 한다.
- 선택된 실행 상세는 workflow run 전체 요약과 현재 노드 기록을 구분해서 표시해야 한다.
- 노드 실행 기록 패널은 현재 노드 설정을 변경하지 않는 read-only 참고 UI다.

### MBA-190 리뷰 보강

- FR-030 보강: 기존 terminal 또는 만료된 prepared attempt를 현재 delivery가 다시 claim하면 stable effect identity와 frozen contract는 유지하되 nullable `workflow_run_id`와 `node_run_id`는 현재 `ExternalEffectContext` 값으로 갱신해야 한다.
- FR-031 보강: GitHub issue comment의 완전한 `401` 인증 거부도 `403`, `404`, `410`, `422`와 같은 `failed_before_effect + stop` 확정 거부다.
- FR-048 보강: Canvas 전용 `note` node는 Workflow Engine이 실행하지 않는 보조 요소이므로 legacy child graph의 external-effect classifier에서도 명시적으로 무시해야 한다.
- Stream retry 보강: external-effect retry signal이 Celery max retries를 소진하면 stream task는 원래 safe code, `retryable=false`, optional `node_id`를 포함한 Pub/Sub `error` event를 한 번 발행한 뒤 최종 task 실패를 전파해야 한다. 재시도 예산이 남아 `celery.exceptions.Retry`가 발생한 delivery는 terminal event를 발행해서는 안 된다.
- FR-028 보강: legacy `workflow.execute_deployed` redelivery는 envelope의 exact deployment row/version/hash를 검증해 실행해야 한다. Current active deployment는 같은 app의 실제 active row인지 확인하는 kill switch이며 frozen row를 대체해서는 안 된다.
- FR-031 보강: repository가 terminal commit 시 replay deadline을 다시 평가해 `stop`으로 낮추면 application은 commit 전 지역 변수가 아니라 반환된 `terminal.replay_decision`을 따라야 한다. 완전한 non-JSON HTTP 응답의 UTF-8 decode 실패는 기존 text output으로 처리해야 한다.
- FR-035/FR-050 보강: Generic HTTP, Slack, GitHub comment의 downstream output과 사용자 응답은 유지하되 durable node input, process data, output, node trace payload, `WorkflowRun.outputs`와 run-level trace payload에는 provider, operation, method, status, size, latency, outcome, replay decision, safe error code만 저장한다. Provider 결과를 전달받는 downstream node는 durable node input/process/output과 run-level output을 저장하지 않는다. 중첩 Workflow/Loop는 결과 민감도를 완료 뒤 판정하므로 시작 로그에 입력·설정 payload를 저장하지 않고, 외부 결과가 없는 안전한 성공 finish에서만 보류 값을 기록해야 한다. 외부 결과가 있으면 server-owned 민감 표시를 부모 실행으로 전달해 컨테이너 노드와 그 downstream에도 같은 정책을 적용한다. Workflow Engine과 Log System이 같은 node-aware 정책을 각각 적용하고, 실제 `process_data.node_options` 구조를 판별하며, 실패 경로도 adapter allowlist summary를 보존해야 한다.
- DB 시간 보강: lease와 replay deadline은 row lock 획득 후 PostgreSQL `clock_timestamp()`로 판정해야 한다. Downgrade는 attempt table을 `ACCESS EXCLUSIVE`로 잠근 같은 transaction에서 empty를 확인한 뒤에만 drop을 진행한다.
- CI 보강: 실제 PostgreSQL external-effect migration, readiness, unique slot, lock/lease 경쟁 증거 테스트는 일반 PR PostgreSQL workflow에서 실행해야 한다.

## Policies And Edge Cases

### 1. 실행 편의성

- 노드별 토큰 사용량은 `node_finish.total_tokens` 표준 필드를 우선 사용한다. 값이 없으면 `-`로 표시한다.
- 노드별 비용은 `node_finish.total_cost` 표준 필드를 우선 사용한다. 값이 없으면 `-`로 표시한다.
- 노드별 소요 시간은 `node_finish.latency_ms` 표준 필드를 우선 사용한다. 값이 없으면 프론트가 `node_start`와 `node_finish` 이벤트 수신 시각 기준으로 계산한 값을 fallback으로 사용할 수 있다.
- 프론트의 fallback 소요 시간은 백엔드의 영구 실행 로그와 완전히 같은 값이라고 보장하지 않는다.
- 서버 실행 시간과 화면 완료 시간은 서로 다른 지표다. 화면 완료 시간에서 서버 실행 시간을 뺀 값을 순수 UI 처리 시간으로 표현하지 않는다.
- workflow-level 서버 실행 시간이 응답에 없으면 노드별 `latency_ms` 합산값을 서버 실행 시간 fallback으로 표시한다. 노드 latency도 없을 때만 서버 실행 시간은 `-` 또는 `기록 없음`으로 표시한다.
- 실행 권한이 없는 사용자는 테스트 버튼을 실행할 수 없어야 하며, 테스트 실행 사이드바는 이전 결과를 조작 가능한 상태로 보여주지 않는다.
- secret, credential 원문, raw prompt 전체를 테스트 실행 요약에 노출하지 않는다.
- 최종 응답 카드의 preview는 hidden KB id, source title/path/url, credential, raw trace payload 같은 내부 식별자와 민감 payload를 표시하지 않는다.
- 최종 응답이 비어 있으면 빈 상태를 명확히 표시하고, 실패 상태에서는 성공 결과용 최종 응답 카드 대신 실패 메시지를 유지한다.

### 2. 노드 조작 편의성

- 패널 리사이즈는 프론트 UI 상태이며 API나 저장된 workflow graph를 변경하지 않는다.
- 패널 폭 계산은 grid 자체의 현재 폭이 아니라, 노드 상세 편집 영역을 감싸는 부모 영역의 실제 가로 폭을 기준으로 한다.
- 사용자가 아직 직접 드래그하지 않았다면 화면 크기 변경 시 기본 비율을 다시 적용한다. 사용자가 드래그한 뒤에는 현재 편집 세션의 사용자 조정 폭을 유지한다.
- 작은 화면에서는 3패널 리사이즈보다 기존 responsive layout을 우선한다. 최소 폭을 만족할 수 없는 경우 resizer를 숨기거나 기본 비율로 고정한다.
- 드래그 중 텍스트 선택, 캔버스 pan/zoom, 노드 편집 입력이 의도치 않게 발생하지 않아야 한다.

### 3. 워크플로우 조작 편의성

- `뒤에 추가`는 노드 생성, local placement, edge 생성이 하나의 graph edit 동작으로 처리되어야 하며 undo 한 번으로 되돌릴 수 있어야 한다.
- `뒤에 추가` edge 생성은 일반 edge 연결과 같은 validation 경로를 사용해야 한다. 검증에 실패하면 노드와 edge를 모두 만들지 않는다.
- 자동 배치 위치는 선택된 노드 오른쪽을 기본으로 하되, 기존 노드와 겹치면 같은 흐름을 해치지 않는 범위에서 아래 또는 오른쪽으로 밀어 배치한다.
- `뒤에 추가`는 사용자 편의를 위한 graph edit 동작이며 API를 호출하지 않는다. 저장은 기존 workflow draft sync 경로를 따른다.
- 키보드 삭제는 캔버스가 shortcut scope일 때만 동작한다. 입력창, textarea, select, contenteditable 내부에서는 Backspace/Delete가 노드 삭제로 해석되면 안 된다.
- 레이아웃 최적화는 하단 툴바의 레이아웃 최적화 버튼으로 실행할 수 있어야 한다.
- 시작 트리거 노드처럼 삭제가 제한된 노드가 있다면 기존 삭제 가능 정책을 우선한다.
- 자동 재연결은 사용자 편의를 위한 graph edit 동작이며 API를 호출하지 않는다. 저장은 기존 workflow draft sync 경로를 따른다.

### 4. 노드 실행 기록 패널 추가

- 노드 실행 기록은 workflow run, node run, trace payload, LLM usage log에 저장된 기존 실행 데이터를 읽어 표시한다.
- secret, credential 원문, raw prompt 전체, raw payload 중 정책상 비공개 값은 패널에 노출하지 않는다.
- 실행 목록은 기본적으로 최신순으로 표시한다. 많은 실행 로그가 있는 경우 초기 로드는 제한된 개수만 수행하고 `더 보기`로 확장한다.
- 현재 node_id 기록이 없는 workflow run은 목록에서 제외하거나, 표시하더라도 `이 실행에서 현재 노드 기록 없음`으로 명확히 구분한다.
- 최신 기록 불러오기는 workflow의 최신 run 1개가 아니라, 현재 node_id 기록이 존재하는 최신 run을 대상으로 한다.

### 5. 교차 도메인 실행 경계

- Catalog required configuration preflight는 `external_read`, `external_write`, `local_execution` node를 동일한 server-derived 규칙으로 검사한다. client가 보낸 `configuration_state`는 신뢰하지 않으며 draft 편집·저장은 허용하되 실행·배포 admission은 unresolved이면 차단한다.
- `WorkflowNode`의 실행 필수 reference는 `appId`이며 `workflowId`는 선택 metadata다. Runtime model과 NodeFactory도 누락된 `workflowId`를 빈 metadata로 정규화한다. `loopNode`는 `subGraph`를 필수로 검사하되, `loop_key`가 없거나 빈 값일 때 mapped input의 첫 배열을 반복 대상으로 사용하는 기존 runtime fallback을 유지한다. Loop body의 implicit entry만 `loop.item|index`, 상위 실행 입력, Loop까지 방향성 선행 경로가 있는 현재 graph node output과 명시적 mapped input을 직접 selector source로 사용할 수 있다. Body 후속 노드는 완료된 local predecessor output만 사용하며 inherited source를 직접 참조하거나 downstream nested Loop의 child frame으로 재전달하면 fail-closed한다. Parent graph의 후행·형제 node output과 stale input mapping도 child source로 등록하지 않는다. Configuration 순회도 shared graph depth 상한 16을 넘으면 fail-closed해야 한다.
- test, run, deployment와 schedule은 동일한 Catalog 기반 configuration 판정을 사용한다. Gateway schedule dispatch는 task publish 전에 configuration과 DB 기반 deployment target/policy를 검사한다. Worker schedule admission은 locked canonical root identity와 같은 configuration 판정을 다시 검사하고, publish 뒤 변경될 수 있는 WorkflowNode/KB/credential 권한과 상태는 각 runtime의 authoritative gate가 다시 검사한다. Workflow Engine이 Gateway application을 직접 import하거나 같은 DB 정책을 복제해서는 안 된다.
- worker preflight blocker는 기존 `configuration_preflight_blocked` safe reason으로 `dispatching|enqueued -> canceled` 처리하고 `schedule_dispatch.canceled` audit을 남긴다. `workflow_run_id`, `started_at`, Knowledge sync, engine/provider 호출과 Celery retry는 시작하지 않는다.

- 생성된 workflow나 Agent Builder가 만든 workflow도 일반 workflow와 동일한 organization scope, RBAC, audit, trace 정책을 따른다.
- Workflow 실행 권한, LLM credential `use`, connector/connection 사용 권한, Knowledge KB/source ACL 권한은 서로를 대체하지 않는다.
- Workflow-node 순환 참조, nesting depth 초과, target app/deployment unavailable 같은 복구 불가능한 graph 설정 오류는 retry 가능한 일시 장애가 아니다. Celery task는 이러한 non-retryable runtime error를 즉시 실패로 보존해야 한다.
- Workflow runtime HTTP/GitHub node의 전체 outbound egress policy는 [ADR-0014](../../decisions/ADR-0014-knowledge-base-document-atom-and-collection-boundary.md)의 Knowledge source collection egress boundary와 별도 gate다. Mail IMAP 연결은 [ADR-0031](../../decisions/ADR-0031-mail-credential-reference-boundary.md)의 제한된 egress gate를 적용한다.
- GitHub pull request 조회/comment와 Slack API/webhook 호출은 server-owned operation ID, HTTPS/443 endpoint와 bounded request/response/timeout profile을 사용하는 공통 guarded requester를 통과해야 한다. Node와 application service는 직접 `requests`/`httpx` client를 생성하거나 arbitrary provider URL·method를 실행해서는 안 된다.
- 공통 requester는 network 전송과 safe failure phase만 소유한다. GitHub/Slack adapter의 provider path·payload 검증, safe output, external-effect admission, retry 금지와 outcome-unknown 판정은 기존 계약을 유지해야 한다.

- 외부 부수효과 node의 ambiguous provider outcome은 복구 불가능한 설정 오류와 별개지만, 외부 서비스 중복 방지 지원 수준이 `unsupported` 또는 `unknown`이면 자동 replay 가능한 일시 장애로 취급하지 않는다.
- `workflow_node_effect_attempts`는 외부 부수효과 claim과 replay decision의 source of truth다. 비동기 `workflow_runs`/`workflow_node_runs` row는 nullable correlation이며 attempt 생성의 FK 선행 조건이 아니다.
- 같은 logical retry는 stable execution/node invocation identity를 유지해야 하고 정상 Loop iteration과 별도 subworkflow invocation은 다른 identity를 사용해야 한다.
- 이 execution identity 계약은 이미 시작된 logical execution 안의 외부 부수효과를 식별하는 용도다. 서로 다른 API/public/webhook 요청을 같은 admission으로 합치거나 비-schedule trigger 공통 admission ledger를 추가하지 않는다.
- MBA-190의 test-only fake provider는 replay `supported` 중복·장애 구간 검증용이고 fake adapter는 result reuse `supported|unavailable` 검증용이다. Generic HTTP mutation과 method에 무관한 `slackPostNode` request는 1차 production에서 외부 서비스 중복 방지 `unknown`, GitHub `comment_pr`는 `unsupported`이고 세 operation의 결과 재사용은 `unavailable`로 둔다. Generic HTTP `GET`과 GitHub `get_pr`는 새 attempt를 만들지 않는다. Fake는 FR-040의 고정 계약을 사용하며 production 지원 또는 exactly-once 증거가 아니다.
- HTTP 외부 호출 trace/log에는 provider, canonical operation, method, status code, size, latency, 두 지원 수준, outcome/replay decision, safe error code와 Generic HTTP의 기존 목적지 `host`/`path`만 남길 수 있다. Key fingerprint는 operational attempt column에만 허용한다. 전체 URL, query/fragment/URL user info, request/response header/body, 새 identity/key, provider request identifier와 provider exception message 원문은 남기지 않는다.
- Slack `ok=false`, provider error code와 `Retry-After` 처리는 FR-052와 ADR-0037의 전용 adapter가 소유하며 MBA-190 공통 executor는 typed outcome만 처리한다.
- Gmail inbound fetch/claim/terminal acknowledgement와 reply draft는 취소된 MBA-220 범위를 통합한 MBA-217과 [ADR-0032](../../decisions/ADR-0032-mail-processing-gmail-draft-idempotency.md)가 소유한다. 현재 Mail/Gmail 구현은 전용 `mail_message_processings`/`mail_draft_effects` ledger를 사용하며 MBA-190에서 공통 ledger로 이관하거나 이중 기록하지 않는다. 직접 adapter 정렬은 ADR-0032 갱신이 필요한 후속 범위다.
- External effect identity/lifecycle rule, application orchestration, SQLAlchemy repository와 provider network 코드는 [ADR-0035](../../decisions/ADR-0035-external-effect-idempotency-boundary.md)과 `docs/architecture.md`의 package 경계를 따른다.

### Mail Credential Reference

- Mail node graph는 organization-scoped Mail credential의 opaque `credential_id`만 저장한다. Email, password, token, ciphertext, provider endpoint를 graph에 직접 저장하지 않는다.
- Workflow 저장 경계는 최상위 graph와 모든 중첩 `subGraph.nodes`의 inline Mail secret field를 거부하고, reference가 있으면 active organization, active 상태와 저장 요청자의 `use` 권한을 검증한다. `displayNumber`와 `visibleProperties`는 제한된 UI metadata만 허용한다.
- Agent Builder가 만든 Mail node는 `credential_id=null`, `configuration_state=unresolved`로 preview와 draft 저장이 가능하지만 실행 준비 상태로 간주하지 않는다. Deployment snapshot 생성과 기존 deployment 활성화는 최상위와 중첩 Mail node 모두 유효한 credential reference가 있어야 한다.
- 인증 test/deployment run은 명시 user execution subject와 canonical organization을 runtime resolver에 전달한다. Resolver는 provider 연결 직전에 scope, active 상태, `use` 권한, egress target을 재검증한다. Public/schedule run은 App/workflow owner를 대체 주체로 사용하지 않으며, service account 또는 assigned operator 정책이 없으면 Mail 실행을 차단한다.
- Credential이 없거나 revoke됐거나 권한이 회수된 경우 Mail provider 연결 전에 safe error로 fail-closed한다. Legacy inline password graph는 호환 fallback 없이 거부한다.
- Mail node의 `processing_mode` 기본값은 기존 호환을 위한 `search_only`다. `durable` mode는 workflow/source node scoped processing row를 생성하고 opaque `processing_ref`를 출력한다.
- 단일 Gmail Draft node에 직접 연결하는 durable Mail node는 `max_results=1`이어야 하며 최상위 `processing_ref`를 제공한다. 여러 메일 결과는 명시적인 반복 처리 계약 없이 단일 Draft selector에 연결하지 않는다.
- Durable mode에서는 Mail node의 `mark_as_read=true`를 금지한다. `mailAcknowledgeNode`가 required effect의 durable success를 검증한 뒤에만 원본 메시지를 처리 완료한다.
- Gmail 답장 초안은 별도 `gmailDraftNode`가 수행한다. Node는 `credential_id`, `processing_ref` selector, reply body selector만 저장하고 provider id, token, MIME 또는 recipient override를 graph에 저장하지 않는다.
- Gmail Draft effect가 `outcome_unknown`이면 Workflow/Celery generic retry가 provider create를 자동 재호출하지 않아야 한다.
- `mailAcknowledgeNode`는 graph dependency로 연결된 processing/effect reference를 서버에서 검증하며 client 제공 success boolean을 신뢰하지 않는다.
- Gmail Draft 또는 Mail Acknowledge가 포함된 graph는 source Mail node가 durable mode인지와 required selector 연결을 save/deploy 경계에서 검증한다.
- 편집 중 unresolved Draft/Acknowledge node의 빈 selector는 저장할 수 있지만 save/deploy 경계에서는 source/effect node 유형, 출력 key, 선행 graph path, source/Draft credential 일치와 Gmail OAuth auth type을 검증한다. Workflow Engine은 legacy snapshot 실행 시 selector schema와 DB processing/deployment/credential provenance를 다시 검증하여 fail-closed한다.
- 기존 활성 Draft claim을 발견한 중복 실행은 claim을 획득한 것으로 간주하지 않으며 Gmail provider를 다시 호출하지 않는다.
- OAuth Gmail Mail node와 terminal acknowledgement는 `gmail.modify` 기반 고정 Gmail REST adapter를 사용한다. OAuth credential을 IMAP XOAUTH2로 연결하지 않으며 app password/password credential만 제한된 IMAP 경로를 사용한다. Production proxy mode의 IMAP은 검증 IP를 Worker-only `3129` CONNECT authority로 사용하고 원래 hostname으로 TLS를 검증하며 proxy 실패 시 direct fallback하지 않는다.
- Gmail REST provider message id는 암호화된 processing source reference에만 저장하고 node output, API, audit, trace, log에는 노출하지 않는다.
- Mail body/subject/recipient와 생성 답장 본문은 runtime graph 데이터로만 전달한다. Durable node trace는 Mail result와 Gmail effect input을 node-aware safe summary로 치환한 뒤 일반 trace redaction/retention 정책을 적용한다.
- RAG를 포함한 workflow 비교 실행이나 A/B 실행도 로그인 interactive 실행이면 동일한 execution subject와 Knowledge permission/source ACL gate를 사용하고, subject가 없으면 anonymous public-only gate를 사용한다.
- 배포 preflight에서 client-supplied audience hint는 preview UI용이며, create/activation 경로는 deployment type과 실행 endpoint에서 audience를 서버가 다시 파생한다.
- Private KB를 자동 실행에서 사용하려면 별도 service account 또는 assigned operator 정책이 필요하다. 이 정책이 없으면 workflow owner, deployment owner, app creator 권한으로 fallback하지 않는다.
- Anonymous public-only gate에서 source-managed KB는 collection public visibility와 별도 source/connector public exposure approval을 모두 통과해야 한다 ([ADR-0020](../../decisions/ADR-0020-knowledge-mcp-incremental-sync-boundary.md)).
- 별도 RAG node를 만들지 않는다. Knowledge retrieval은 LLM node의 RAG option/runtime path로 연결한다 ([ADR-0017](../../decisions/ADR-0017-knowledge-integration-provisional-implementation-baseline.md)).
- Skill이 workflow generation이나 실행 시점 RAG procedure를 안내하더라도, skill은 data access 권한을 부여하지 않는다. 실제 evidence retrieval은 Knowledge permission helper 결과로만 수행한다.
- Code-bearing skill은 별도 sandbox/approval/egress/resource-cap gate 전까지 workflow runtime에서 실행하지 않는다.
- Workflow Playground가 별도 실험 공간인지 canvas와 통합되는지, draft/unpublished skill을 테스트 실행에 사용할 수 있는지는 아직 확정하지 않는다.
- Missing execution subject는 anonymous public-only로 처리한다. Ambiguous execution subject, suspended/removed membership, inactive service account는 private retrieval fail-closed로 처리한다.


## LLM Grounding And Citation Display

- `answerGroundingCheck`는 `off|basic|strict` lexical overlap metadata를 계산하는 옵션이며, 사용자 Citation 표시나 답변 차단을 의미하지 않는다. UI 명칭은 `답변·검색 문서 어휘 일치도`로 표시한다.
- `citationDisplayMode`는 사용자 응답에서 Citation을 표시하는 독립 옵션이며 `hidden|basic|detailed`를 사용한다.
- 저장된 기존 node에 `citationDisplayMode`가 없으면 `hidden`으로 해석해 응답 계약을 보존한다. 새 수동 node와 Agent Builder 생성 node는 `detailed`를 명시적으로 저장한다.
- Citation은 Answer node의 data dependency ancestry에 있는 LLM node에서만 모은다. 연결되지 않은 control branch와 subworkflow output의 Citation을 자동 승격하지 않는다.

## Open Questions

Open Question 중요도는 다음 3단계로 나눈다.

- `Priority 1`: 현재 기능 구현 또는 데모 핵심 흐름을 막는 결정이다. 구현 전에 먼저 정해야 한다.
- `Priority 2`: 데모 안정성과 후속 구현 품질에 영향을 준다. 현재 구현은 fallback으로 진행할 수 있지만 PR 전후로 정리해야 한다.
- `Priority 3`: 장기 사용성, 성능, 확장성 결정이다. 현재 구현을 막지는 않으며 후속 이슈로 분리할 수 있다.

| Priority | 영역 | Question | 왜 중요한가 | 결정 전 임시 처리 |
| --- | --- | --- | --- | --- |
| 1 | 실행 편의성 | 백엔드 스트리밍 완료 이벤트에 workflow-level `run_id`, `duration`, `total_tokens`, `total_cost`를 표준 필드로 포함할지 여부 | 서버 실행 시간과 비용/토큰을 테스트 실행 사이드바에서 정확히 표시하려면 완료 이벤트 또는 후속 조회 기준이 필요하다. | 완료 이벤트에 값이 없으면 서버 실행 시간은 노드별 `latency_ms` 합산으로 fallback하고, 비용/토큰도 노드별 합산으로 fallback한다. |
| 1 | 워크플로우 조작 편의성 | branching/condition/loop 같은 특수 노드 삭제 시 자동 재연결을 어디까지 허용할지 여부 | 잘못 재연결하면 workflow 의미가 바뀔 수 있다. 삭제 shortcut의 안전성에 직접 영향이 있다. | 기존 연결 검증을 통과하는 단순 upstream/downstream 조합만 재연결하고, 애매한 특수 노드는 삭제만 수행한다. |
| 2 | 노드 실행 기록 패널 추가 | input/output full payload 표시 범위와 redaction 기준을 어디까지 프론트에서 보완할지 여부 | 실행 기록 패널은 trace payload와 LLM usage를 보여줄 수 있어 secret/raw prompt 노출 위험이 있다. | Gateway/API 응답 정책을 우선하고, 프론트는 allowlist 기반 preview/detail 렌더링만 허용한다. |
| 2 | 노드 실행 기록 패널 추가 | node_id 기반 실행 기록 API의 cursor 기준을 `started_at + run_id`로 둘지, 별도 node_run id 기준으로 둘지 여부 | 실행 로그가 많아질 때 중복/누락 없는 pagination과 최신 기록 선택에 영향을 준다. | 초기 구현은 제한된 최신 page와 명확한 정렬 기준을 사용하고, 대량 조회는 후속 API에서 cursor를 확정한다. |
| 2 | 노드 실행 기록 패널 추가 | input/output 검색을 preview 문자열 기준으로 제한할지, redaction-safe full payload 검색까지 허용할지 여부 | 검색 품질과 보안 경계가 충돌할 수 있다. | 우선 preview 문자열 검색으로 제한한다. |
| 3 | 실행 편의성 | A/B 비교 실행 결과를 테스트 실행 사이드바 안에서 확장할지, 별도 비교 패널로 분리할지 여부 | 현재 단일 테스트 실행 UX를 막지는 않지만, 이후 비용/품질 비교 UI 구조에 영향을 준다. | 현재 TestSidebar는 단일 실행 결과만 다루고 A/B 비교 UI는 별도 후속 설계로 둔다. |
| 3 | 노드 조작 편의성 | 패널 비율을 사용자별 preference로 영구 저장할지 여부 | 반복 작업 편의성에는 도움이 되지만 현재 리사이즈 기능 구현을 막지는 않는다. | 현재 편집 세션 안에서만 유지하고 저장하지 않는다. |

후속 확장 open question:
- Service account의 데이터 접근 범위와 승인 절차를 Auth/RBAC에서 어떤 table과 helper로 표현할지.
- Schedule/webhook/API trigger에서 private KB access가 필요할 때 service account/assigned operator `execution_subject` resolution reason enum과 audit action 이름을 어떻게 둘지. Conversation Access Grant나 credential principal은 후보가 아니다.
- Generic HTTP는 ADR-0050의 Workflow application port와 공통 guard를 사용한다. GitHub/Slack fixed-SaaS는 MBA-356의 operation-bound requester로 이관됐고, ADR-0072/MBA-357은 production public HTTP/HTTPS를 explicit Squid와 proxy-only network 경계에 연결한다. 등록되지 않은 adapter는 별도 outbound inventory에서 추적한다.
- Skill execution을 runtime node로 허용할지, 허용한다면 sandbox와 approval 경계를 어디에 둘지.
- Workflow Playground, canvas 작업 공간/사용 공간, 배포 승인 요청에서 skill binding을 어떻게 표현하고 검토할지.

## Test 실행 저장 경계

- 서로 다른 workflow를 포함한 Agent Builder save/acknowledgement가 겹치면 전역 차단 상태는 진행 중 작업 수를 기준으로 유지하며, 먼저 끝난 작업이 남은 저장의 test/autosync/Undo 차단을 해제해서는 안 된다.
- Agent Builder draft POST 자동 재시도는 transport 단절 또는 `5xx`처럼 적용 결과가 불명확할 때만 같은 mutation context로 한 번 허용한다. 명시적인 `401|403|409|422`는 반복 전송하지 않는다.

- Workflow test preflight는 canonical graph 확인, 필요한 CAS save와 valid `workflow_start.run_id` 수신까지를 workflow별 save coordinator의 단일 직렬화 경계로 처리해야 한다.
- Agent Builder, autosync, Undo/Redo, version restore 또는 다른 test preflight 저장이 대기하거나 진행 중이면 test를 자동 실행하지 않고 저장 완료 후 사용자의 재시도를 요구해야 한다. 실행 요청 뒤 도착한 저장은 `workflow_start.run_id`로 snapshot이 확정될 때까지 대기한다.
- Editor가 clean이어도 server canonical graph와 local snapshot이 다르면 test를 차단해야 하며 최신 metadata를 오래된 local graph와 결합해서는 안 된다.
- Node root의 `width`, `height`, `measured`, `dragging`, `resizing`, `selected`, `positionAbsolute`, node data의 실행 `status`, `observability`, `displayNumber`와 edge selection은 최상위와 중첩 `subGraph.nodes`, `features.noteNodes`의 영속 Workflow graph/hash, autosync dirty state와 Undo/Redo history를 변경해서는 안 된다. Client의 Agent Builder, autosync, version 복원, test 전 저장, Undo/Redo와 note 저장 및 Gateway 저장 경계는 같은 재귀 canonical projection을 적용해야 하며 node `position`과 business configuration은 보존해야 한다.
- Server-derived node `configuration_state`는 Client save payload와 canonical 비교 projection에서 최상위 및 중첩 node 모두 제외한다. Server는 Catalog 기준으로 다시 계산해 저장 graph와 canonical response에 materialize해야 한다.
- Version restore는 snapshot의 `features.noteNodes` field가 있으면 이를 권위로 사용하고 명시적 빈 배열을 전체 Note 삭제로 처리해야 한다. 해당 field가 없는 legacy snapshot은 `nodes`의 Note를 사용하며, 두 표현이 모두 없으면 현재 editor Note를 보존해야 한다.
